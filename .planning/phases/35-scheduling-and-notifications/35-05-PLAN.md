---
phase: 35-scheduling-and-notifications
plan: 05
type: execute
wave: 3
depends_on: ["35-04"]
files_modified:
  - apps/portal/src/components/basel/calendar/create-event-modal.tsx
  - apps/portal/src/components/basel/calendar/calendar-week-view.tsx
  - apps/portal/src/components/basel/calendar/calendar-event-detail.tsx
  - apps/portal/src/components/basel/calendar/calendar-context.tsx
  - apps/portal/src/app/portal/calendar/page.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking empty time slot on calendar opens CreateEventModal with pre-filled time"
    - "CreateEventModal has 'Link to application' toggle that enables interview creation"
    - "Interview events have distinct styling on the calendar (unique color/icon)"
    - "Calendar event detail panel shows interview info with join button when call window is active"
    - "Scheduling from calendar prompts to move application to interview stage if needed"
  artifacts:
    - path: "apps/portal/src/components/basel/calendar/create-event-modal.tsx"
      provides: "Event modal with application linking"
      contains: "linkToApplication"
    - path: "apps/portal/src/components/basel/calendar/calendar-event-detail.tsx"
      provides: "Event detail with interview info"
      contains: "Join Interview"
  key_links:
    - from: "apps/portal/src/components/basel/calendar/create-event-modal.tsx"
      to: "apps/portal/src/components/basel/scheduling/application-search.tsx"
      via: "renders ApplicationSearch when linking"
      pattern: "ApplicationSearch"
    - from: "apps/portal/src/components/basel/calendar/calendar-week-view.tsx"
      to: "apps/portal/src/components/basel/calendar/calendar-context.tsx"
      via: "openCreate with pre-filled time on slot click"
      pattern: "openCreate"
---

<objective>
Enhance the calendar page with slot-click scheduling, application linking in CreateEventModal, interview event styling, and rich event detail panel with interview actions.

Purpose: Enable scheduling from the calendar page and visually distinguish interview events.
Output: Updated calendar components with interview awareness.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-scheduling-and-notifications/35-CONTEXT.md
@apps/portal/src/components/basel/calendar/create-event-modal.tsx
@apps/portal/src/components/basel/calendar/calendar-week-view.tsx
@apps/portal/src/components/basel/calendar/calendar-event-detail.tsx
@apps/portal/src/components/basel/calendar/calendar-context.tsx
@apps/portal/src/app/portal/calendar/page.tsx
@apps/portal/src/components/basel/scheduling/application-search.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add slot-click scheduling and application linking to CreateEventModal</name>
  <files>apps/portal/src/components/basel/calendar/calendar-context.tsx, apps/portal/src/components/basel/calendar/calendar-week-view.tsx, apps/portal/src/components/basel/calendar/create-event-modal.tsx, apps/portal/src/app/portal/calendar/page.tsx</files>
  <action>
1. **In calendar-context.tsx:**
   - Add `prefillTime: { date: string, startTime: string } | null` to context value
   - Add `openCreateWithTime: (date: string, startTime: string) => void` — sets prefillTime and opens create modal
   - Update `closeCreate` to also clear prefillTime

2. **In calendar-week-view.tsx:**
   - Add click handler on empty time slots (existing grid cells that have no event)
   - On click, compute the date and time from the cell position
   - Call `openCreateWithTime(date, startTime)` from calendar context
   - Visual feedback: cursor-pointer on empty cells, subtle hover state `hover:bg-primary/5`

3. **In create-event-modal.tsx:**
   - Accept new optional props: `prefillDate?: string`, `prefillStartTime?: string` (from context)
   - Add "Link to Application" toggle at the top of the form (DaisyUI toggle: `<input type="checkbox" className="toggle toggle-primary" />` with label)
   - When toggle is ON:
     - Render `ApplicationSearch` component (from scheduling sub-components)
     - On application selection, auto-switch event type display to show "Interview" badge
     - Auto-set title to `Interview: [Candidate Name] — [Job Title]` (non-editable)
     - Auto-add candidate email to attendees
     - Show `PlatformSelector` component
     - If selected platform is 'splits_video': on submit, create calendar event AND interview record (POST to `/interviews`)
     - If platform is Meet/Teams: only create calendar event
     - If application is NOT at 'interview' stage: show confirmation dialog "Move [Candidate] to interview stage?" with Yes/No. On Yes, PATCH application stage to 'interview' before creating event
   - When toggle is OFF: existing behavior (just create calendar event)
   - Pre-fill date and time from prefillDate/prefillStartTime if provided
   - Default duration 30 minutes when linked to application

4. **In page.tsx:**
   - Pass `prefillDate` and `prefillStartTime` from calendar context to CreateEventModal when it renders
  </action>
  <verify>`cd apps/portal && npx tsc --noEmit` passes.</verify>
  <done>CreateEventModal supports application linking, slot-click pre-fills time, and handles interview creation with platform choice and stage promotion.</done>
</task>

<task type="auto">
  <name>Task 2: Interview event styling and rich detail panel</name>
  <files>apps/portal/src/components/basel/calendar/calendar-week-view.tsx, apps/portal/src/components/basel/calendar/calendar-agenda-view.tsx, apps/portal/src/components/basel/calendar/calendar-event-detail.tsx</files>
  <action>
1. **Interview event detection:**
   - An event is an interview if its summary starts with "Interview:" — simple string check
   - Define a helper function `isInterviewEvent(event: CalendarEvent): boolean` in calendar-context.tsx or a shared util

2. **In calendar-week-view.tsx:**
   - When rendering event blocks, check `isInterviewEvent(event)`
   - Interview events: `bg-accent text-accent-content` background (distinct from default `bg-primary/10`)
   - Add a small video icon `fa-video` before the event title for interview events
   - Apply `border-l-4 border-accent` left accent on interview event blocks

3. **In calendar-agenda-view.tsx:**
   - Same interview detection and distinct styling
   - Show "Interview" badge next to time for interview events using `badge badge-accent`

4. **In calendar-event-detail.tsx** (the sidebar detail panel when an event is selected):
   - If `isInterviewEvent(selectedEvent)`:
     - Show interview-specific info section:
       - Extract candidate name and job title from the event summary ("Interview: [Candidate] — [Job]")
       - Show candidate name and job title in a styled info card with `border-l-4 border-accent`
     - "Join Interview" button:
       - Show when the interview is within 10 minutes of start time or currently in progress
       - Button: `btn btn-accent` with `fa-video` icon
       - Clicking opens the interview page in a new tab (use `window.open`)
       - To get the interview ID: look up interview by matching calendar event ID. Make a fetch to `/interviews?calendar_event_id={eventId}` — add this query param support to the list endpoint
     - "Reschedule" and "Cancel" action buttons (below join button):
       - Reschedule: opens the ScheduleInterviewModal pre-filled with current details (implementation in plan 07)
       - Cancel: opens a confirmation dialog (implementation in plan 07)
       - For now, render the buttons but make them call `console.log('TODO: implement in plan 07')` as placeholder
   - Non-interview events: existing detail panel behavior unchanged
  </action>
  <verify>`cd apps/portal && npx tsc --noEmit` passes. Calendar renders interview events with distinct accent color and icon.</verify>
  <done>Interview events have distinct visual styling on the calendar. Detail panel shows interview info and join button when call window is active.</done>
</task>

</tasks>

<verification>
- Slot click opens CreateEventModal with correct pre-filled time
- Application linking toggle works and auto-generates interview title
- Interview events render with accent color and video icon
- Event detail panel shows interview info for interview events
- All text minimum `text-sm`, no rounded corners
</verification>

<success_criteria>
Calendar page fully supports interview scheduling: slot-click opens modal, events link to applications, interview events are visually distinct, and detail panel shows interview-specific actions.
</success_criteria>

<output>
After completion, create `.planning/phases/35-scheduling-and-notifications/35-05-SUMMARY.md`
</output>
