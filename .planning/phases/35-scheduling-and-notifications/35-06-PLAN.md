---
phase: 35-scheduling-and-notifications
plan: 06
type: execute
wave: 3
depends_on: ["35-03"]
files_modified:
  - apps/portal/src/app/portal/integrations/components/calendar-preferences-panel.tsx
  - apps/portal/src/app/portal/integrations/page.tsx
  - apps/portal/src/app/portal/applications/components/shared/actions-toolbar.tsx
autonomous: true

must_haves:
  truths:
    - "Users can configure working hours and working days on the integrations page"
    - "Users can select which calendars are included in free/busy checks"
    - "Moving application to interview stage shows a toast with link to schedule"
    - "Stage change toast appears once on transition, not on every render"
  artifacts:
    - path: "apps/portal/src/app/portal/integrations/components/calendar-preferences-panel.tsx"
      provides: "Working hours and calendar selection configuration"
      contains: "workingHours"
    - path: "apps/portal/src/app/portal/applications/components/shared/actions-toolbar.tsx"
      provides: "Stage change interview toast"
      contains: "interview"
  key_links:
    - from: "apps/portal/src/app/portal/integrations/components/calendar-preferences-panel.tsx"
      to: "/api/interviews/calendar-preferences"
      via: "saves preferences to backend"
      pattern: "calendar-preferences"
---

<objective>
Add working hours configuration on the integrations page and implement the stage-trigger toast that prompts users to schedule an interview when moving an application to the interview stage.

Purpose: Supporting features for the scheduling flow — working hours drive slot computation, toast drives discovery.
Output: Calendar preferences panel and stage change toast.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-scheduling-and-notifications/35-CONTEXT.md
@apps/portal/src/app/portal/integrations/page.tsx
@apps/portal/src/app/portal/applications/components/shared/actions-toolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create calendar preferences panel on integrations page</name>
  <files>apps/portal/src/app/portal/integrations/components/calendar-preferences-panel.tsx, apps/portal/src/app/portal/integrations/page.tsx, services/video-service/src/v2/interviews/routes.ts, services/video-service/src/v2/interviews/repository.ts</files>
  <action>
1. **Backend — add calendar preferences endpoints in video-service routes.ts:**
   - `GET /api/v2/interviews/calendar-preferences` — authenticated. Returns user's calendar preferences from `user_calendar_preferences` table. If none exist, return defaults.
   - `PUT /api/v2/interviews/calendar-preferences` — authenticated. Upsert user's preferences. Body: `{ connection_id?, primary_calendar_id?, additional_calendar_ids?, working_hours_start, working_hours_end, working_days, timezone }`.

   Add repository methods:
   - `getCalendarPreferences(userId)` — select from user_calendar_preferences where user_id matches
   - `upsertCalendarPreferences(userId, prefs)` — upsert using ON CONFLICT (user_id)

   Add gateway proxy routes for these endpoints.

2. **Create calendar-preferences-panel.tsx:**
   - Renders as a section within the integrations page (below the installed integrations)
   - Only shown when user has at least one active calendar connection
   - Section header: kicker "SCHEDULING PREFERENCES" with `text-sm font-semibold uppercase tracking-[0.2em] text-primary`

   **Working Hours section:**
   - Start time: `<select>` with 30-min increments from 06:00 to 12:00
   - End time: `<select>` with 30-min increments from 12:00 to 22:00
   - Same hours every day (per CONTEXT.md decision)
   - Working days: 7 checkboxes (Mon-Sun) with DaisyUI `checkbox checkbox-sm checkbox-primary`
   - Timezone: `<select>` populated from `Intl.supportedValuesOf('timeZone')` with current timezone pre-selected

   **Calendar Selection section:**
   - "Calendars included in free/busy checks"
   - List user's calendars from their primary connection
   - Each calendar: checkbox to include/exclude
   - Primary calendar checked and labeled "(Primary)" by default

   **Save button:**
   - `btn btn-primary` — saves via PUT to calendar-preferences endpoint
   - Show success toast on save using DaisyUI toast pattern
   - Loads existing preferences on mount

3. **In integrations page.tsx:**
   - Import and render `CalendarPreferencesPanel` below the existing installed tab content
   - Pass `connections` prop so it can determine if calendar is connected
  </action>
  <verify>`cd apps/portal && npx tsc --noEmit` passes. `cd services/video-service && npx tsc --noEmit` passes.</verify>
  <done>Users can configure working hours, working days, and calendar selection on the integrations page. Preferences are persisted to the database.</done>
</task>

<task type="auto">
  <name>Task 2: Stage trigger toast for interview scheduling</name>
  <files>apps/portal/src/app/portal/applications/components/shared/actions-toolbar.tsx</files>
  <action>
Add interview scheduling toast when application moves to "interview" stage:

1. **In actions-toolbar.tsx** (or whichever component handles stage changes):
   - Find the existing stage change handler/callback
   - After a successful stage change to "interview", show a toast notification:
     - Use DaisyUI toast pattern: `<div className="toast toast-end">` with `alert alert-info`
     - Message: "Don't forget to schedule an interview"
     - Include a link/button: "Schedule Now" that opens the ScheduleInterviewModal
     - Auto-dismiss after 8 seconds
     - Use a ref or state flag to ensure the toast shows only once per stage transition (not on re-renders)

2. **Implementation approach:**
   - After the PATCH to update application stage succeeds AND new stage is "interview":
     - Set a `showScheduleToast` state to true
     - Render the toast conditionally
     - On toast dismiss or "Schedule Now" click, set back to false
     - On "Schedule Now" click: also open the ScheduleInterviewModal (set `showScheduleModal` state)

3. **Render ScheduleInterviewModal** when `showScheduleModal` is true:
   - Pass current application's `candidateName`, `candidateEmail`, `jobTitle`, `applicationId`
   - On close/success: reset `showScheduleModal` to false

Make sure the toast does NOT appear when the page loads and application is already at "interview" stage — only on the transition action itself.
  </action>
  <verify>`cd apps/portal && npx tsc --noEmit` passes. Stage change to "interview" triggers the toast.</verify>
  <done>Moving application to interview stage shows a toast with "Schedule Now" link that opens the scheduling modal. Toast appears once per transition only.</done>
</task>

</tasks>

<verification>
- Calendar preferences panel renders on integrations page
- Working hours and days save correctly
- Stage change to "interview" shows toast
- Toast auto-dismisses and doesn't reappear on re-render
- No `rounded-*` classes, minimum `text-sm` text
</verification>

<success_criteria>
Working hours are configurable and persisted. Stage trigger toast drives users toward scheduling. Both features support the main scheduling flow.
</success_criteria>

<output>
After completion, create `.planning/phases/35-scheduling-and-notifications/35-06-SUMMARY.md`
</output>
