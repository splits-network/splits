---
phase: 26-company-card-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/portal/src/app/portal/companies/types.ts
  - apps/portal/src/app/portal/companies/components/shared/helpers.ts
  - apps/portal/src/app/portal/companies/components/grid/grid-card.tsx
  - apps/portal/src/app/portal/companies/components/grid/grid-card-stats.tsx
autonomous: true

must_haves:
  truths:
    - "Company grid card header shows industry kicker, hiring badge (green when open_roles_count > 0), logo, name, location, and founded year"
    - "Company grid card stats row shows 4 stats: Open Roles, Size, Stage, Avg Salary with colored icon squares"
    - "Company grid card shows tagline (not full description) in an About section with line-clamp-2"
    - "Cards still work correctly for both marketplace and my-companies tabs"
  artifacts:
    - path: "apps/portal/src/app/portal/companies/types.ts"
      provides: "Company interface with stage, founded_year, tagline, social URLs, open_roles_count, avg_salary"
      contains: "open_roles_count"
    - path: "apps/portal/src/app/portal/companies/components/grid/grid-card.tsx"
      provides: "Redesigned grid card with header, tagline, stats, footer"
    - path: "apps/portal/src/app/portal/companies/components/grid/grid-card-stats.tsx"
      provides: "4-stat MarketplaceStats row with Open Roles, Size, Stage, Avg Salary"
  key_links:
    - from: "grid-card.tsx"
      to: "types.ts"
      via: "Company interface import"
      pattern: "import.*Company.*from.*types"
    - from: "grid-card-stats.tsx"
      to: "Company.open_roles_count"
      via: "stats rendering"
      pattern: "open_roles_count|avg_salary"
---

<objective>
Update the portal Company type to include all new fields from phases 22-24, then redesign the company grid card with the showcase editorial layout: industry kicker + hiring badge header, logo/name/location/founded, tagline section, and 4-stat row.

Purpose: The company list API now returns stage, founded_year, tagline, social URLs, open_roles_count, and avg_salary, but the portal Company interface and grid card don't use them yet.
Output: Updated types, redesigned grid card matching the showcase pattern.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/portal/src/app/portal/companies/types.ts
@apps/portal/src/app/portal/companies/components/grid/grid-card.tsx
@apps/portal/src/app/portal/companies/components/grid/grid-card-stats.tsx
@apps/portal/src/app/portal/companies/components/shared/helpers.ts
@apps/corporate/src/app/showcase/cards/one/company-card-editorial.tsx
@packages/shared-types/src/models.ts (reference for Company fields)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Company type with new profile fields</name>
  <files>
    apps/portal/src/app/portal/companies/types.ts
    apps/portal/src/app/portal/companies/components/shared/helpers.ts
  </files>
  <action>
Add the following optional fields to the `Company` interface in `types.ts` to match what the API now returns (from phases 22-24):

```typescript
stage?: string;
founded_year?: number;
tagline?: string;
linkedin_url?: string;
twitter_url?: string;
glassdoor_url?: string;
open_roles_count?: number;
avg_salary?: number | null;
```

Keep `stage` as `string` (not the enum from shared-types) since this is a portal display type, not a backend type. The API returns it as a plain string.

In `helpers.ts`, add two new helper functions:
- `companyFoundedYear(item, isMarketplace)` — returns `(item as Company).founded_year` for marketplace, undefined otherwise
- `companyTagline(item, isMarketplace)` — returns `(item as Company).tagline` for marketplace, undefined otherwise

Also add a `formatSalary(avgSalary: number | null | undefined): string` function that formats the integer avg_salary into a display string like "$185k" (divide by 1000, round, prepend $, append k). Return "N/A" if null/undefined.
  </action>
  <verify>Run `pnpm --filter @splits-network/portal build` — no type errors related to Company interface.</verify>
  <done>Company interface includes all 8 new fields. Helper functions exist for founded_year, tagline, and salary formatting.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign grid card header, stats row, and tagline section</name>
  <files>
    apps/portal/src/app/portal/companies/components/grid/grid-card.tsx
    apps/portal/src/app/portal/companies/components/grid/grid-card-stats.tsx
  </files>
  <action>
**grid-card.tsx** — Redesign the card to match the showcase editorial pattern (`company-card-editorial.tsx`). Key changes:

1. **Header band** — Keep `bg-base-300` band. Update kicker row:
   - Left: industry kicker (already exists, keep as-is)
   - Right: For marketplace tab, show a hiring badge when `company.open_roles_count > 0` — green dot + "Hiring" text (`text-success`). For my-companies tab, keep the existing status badge (Active/Pending/etc).

2. **Avatar + Name block** — Keep existing pattern (logo/initials + "Company" kicker + name). No changes needed.

3. **Location + founded row** — Replace the "added ago" timestamp with founded year when available (marketplace tab). Show `Est. {founded_year}` with a calendar icon. Keep location. For my-companies tab, keep added-ago behavior since founded_year won't be available on the relationship object.

4. **Tagline section** (new, replaces description section) — For marketplace tab, show `company.tagline` (not `description`) with "About" label and `line-clamp-2`. Use the same styling as the current description section. Only render if tagline exists.

5. **Stats row** — Use the updated `MarketplaceStats` (see grid-card-stats.tsx changes below).

6. **Footer** — Keep existing actions toolbar, no changes.

**grid-card-stats.tsx** — Update `MarketplaceStats` to show 4 stats instead of 2:

```typescript
const stats = [
    {
        label: "Roles",
        value: String(company.open_roles_count ?? 0),
        icon: "fa-duotone fa-regular fa-briefcase",
    },
    {
        label: "Size",
        value: company.company_size || "N/A",
        icon: "fa-duotone fa-regular fa-users",
    },
    {
        label: "Stage",
        value: company.stage || "N/A",
        icon: "fa-duotone fa-regular fa-rocket",
    },
    {
        label: "Avg Salary",
        value: formatSalary(company.avg_salary),
        icon: "fa-duotone fa-regular fa-dollar-sign",
    },
];
```

Import `formatSalary` from `../shared/helpers`. The `StatCell` component and `iconStyles` array (primary, secondary, accent, warning) already handle 4 items correctly — no changes needed there. The `RelationshipStats` component stays as-is (2 stats: Role, Access).

Keep text-xs for the stat labels since they are supplementary UI chrome (not primary reading text) and match the showcase pattern.
  </action>
  <verify>Run `pnpm --filter @splits-network/portal build` — no errors. Visually: company grid card should show industry kicker + hiring badge, logo/name, location + founded year, tagline, 4-stat row, actions footer.</verify>
  <done>Grid card displays header with industry kicker and hiring badge, location with founded year, tagline section, and 4-stat row (Open Roles, Size, Stage, Avg Salary). Both marketplace and my-companies tabs render correctly.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @splits-network/portal build` passes with no type errors
2. Company interface in types.ts includes: stage, founded_year, tagline, linkedin_url, twitter_url, glassdoor_url, open_roles_count, avg_salary
3. Grid card header shows hiring badge (marketplace) or status badge (my-companies)
4. Grid card shows founded year instead of "added ago" for marketplace tab
5. Grid card shows tagline (not description) in About section
6. Stats row shows 4 stats with colored icon squares
</verification>

<success_criteria>
- Company type extended with all 8 new API fields
- Grid card matches showcase editorial layout for header, stats, and tagline
- Both marketplace and my-companies tabs render correctly without errors
- Stats row shows Open Roles, Size, Stage, Avg Salary with colored icon backgrounds
</success_criteria>

<output>
After completion, create `.planning/phases/26-company-card-redesign/26-01-SUMMARY.md`
</output>
