---
phase: 48-interview-migration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-gateway/src/index.ts
  - infra/k8s/livekit/livekit-config.yaml
autonomous: true

must_haves:
  truths:
    - "No interview-specific auth-skip rules remain in api-gateway"
    - "LiveKit webhook URL points to the correct /api/v2/calls/recording/webhook handler"
  artifacts:
    - path: "services/api-gateway/src/index.ts"
      provides: "Clean auth-skip rules with no dead interview routes"
      contains: "calls/exchange-token"
    - path: "infra/k8s/livekit/livekit-config.yaml"
      provides: "Correct LiveKit webhook URL"
      contains: "calls/recording/webhook"
  key_links:
    - from: "infra/k8s/livekit/livekit-config.yaml"
      to: "services/video-service/src/v2/calls/call-recording-webhook.ts"
      via: "HTTP webhook URL"
      pattern: "calls/recording/webhook"
---

<objective>
Remove dead interview auth-skip rules from api-gateway and fix the broken LiveKit webhook URL.

Purpose: The gateway has 6 dead auth-skip rules for interview routes that no longer exist (Phase 46 deleted all handlers). The LiveKit config still points to the old `/api/v2/interviews/recording/webhook` URL, meaning recording completion webhooks are hitting a 404. This plan removes the dead rules and fixes the webhook URL.

Output: Clean gateway auth-skip section and working LiveKit webhook config.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-interview-migration-cleanup/48-RESEARCH.md
@services/api-gateway/src/index.ts
@infra/k8s/livekit/livekit-config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead interview auth-skip rules from gateway</name>
  <files>services/api-gateway/src/index.ts</files>
  <action>
    Remove the following 6 dead auth-skip rule blocks from `services/api-gateway/src/index.ts` (approximately lines 476-531):

    1. `POST /api/v2/interviews/join` (line 476-480) — dead, video app uses `/api/v2/calls/exchange-token`
    2. `POST /api/v2/interviews/:id/reschedule-request` (line 495-499) — dead, no handler exists
    3. `GET /api/v2/interviews/:id/available-slots` (line 501-505) — dead, no handler exists
    4. `POST /api/v2/interviews/:id/recording/consent` (line 507-511) — dead, no handler exists
    5. `POST /api/v2/interviews/recording/webhook` (line 513-517) — dead, handler at `/calls/recording/webhook` only
    6. `PUT/GET /api/v2/interviews/:id/notes` (line 525-531) — dead, no handler exists

    Also remove the associated comment blocks (lines 488-493 "Skip auth for video interview magic-link and webhook routes" block).

    KEEP these rules that are NOT dead:
    - `POST /api/v2/calls/exchange-token` (line 482-486) — live, used by video app
    - `POST /api/v2/calls/recording/webhook` (line 519-523) — live, used by LiveKit webhook

    Also update the video-service description string on line 76 from "Internal video interview service for Splits Network" to "Internal video call service for Splits Network".

    IMPORTANT: Do NOT touch any rules outside the interview section. Leave all other auth-skip rules (recruiter-codes, gamification, etc.) untouched.
  </action>
  <verify>
    Run: `grep -n "interviews" services/api-gateway/src/index.ts` — should return zero matches.
    Run: `grep -n "calls/exchange-token\|calls/recording/webhook" services/api-gateway/src/index.ts` — should return the two live call rules.
    Run: `pnpm --filter @splits-network/api-gateway build` — should pass.
  </verify>
  <done>No interview-specific auth-skip rules remain in api-gateway. The two live call auth-skip rules are preserved. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix LiveKit webhook URL to point to call recording handler</name>
  <files>infra/k8s/livekit/livekit-config.yaml</files>
  <action>
    In `infra/k8s/livekit/livekit-config.yaml` line 27, change the webhook URL from:
    ```
    http://video-service:3019/api/v2/interviews/recording/webhook
    ```
    to:
    ```
    http://video-service:3019/api/v2/calls/recording/webhook
    ```

    This fixes a broken webhook — LiveKit egress completion events were hitting a 404 because the old interview route handler was deleted in Phase 46 but the LiveKit config was not updated.
  </action>
  <verify>
    Run: `grep "webhook" infra/k8s/livekit/livekit-config.yaml` — should show `/api/v2/calls/recording/webhook`.
    Run: `grep "interview" infra/k8s/livekit/livekit-config.yaml` — should return zero matches.
  </verify>
  <done>LiveKit webhook URL points to `/api/v2/calls/recording/webhook` matching the actual handler in `services/video-service/src/v2/calls/call-recording-webhook.ts`.</done>
</task>

</tasks>

<verification>
- `grep -rn "interviews" services/api-gateway/src/index.ts` returns nothing
- `grep -rn "interview" infra/k8s/livekit/livekit-config.yaml` returns nothing
- api-gateway builds successfully
</verification>

<success_criteria>
1. Zero interview-specific auth-skip rules in api-gateway/src/index.ts
2. LiveKit webhook URL correctly points to /api/v2/calls/recording/webhook
3. Gateway build passes
</success_criteria>

<output>
After completion, create `.planning/phases/48-interview-migration-cleanup/48-01-SUMMARY.md`
</output>
