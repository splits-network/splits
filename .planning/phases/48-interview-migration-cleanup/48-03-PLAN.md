---
phase: 48-interview-migration-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - services/video-service/src/v2/shared/signed-url-helper.ts
  - services/video-service/src/index.ts
  - services/ai-service/src/v2/call-pipeline/repository.ts
  - services/call-service/src/v2/artifact-repository.ts
  - infra/k8s/video-service/deployment.yaml
  - .env
autonomous: true

must_haves:
  truths:
    - "All code references to interview-recordings bucket now use call-recordings"
    - "Environment variables reference call-recordings bucket name"
    - "K8s deployment uses call-recordings bucket name"
  artifacts:
    - path: "services/video-service/src/v2/shared/signed-url-helper.ts"
      provides: "Signed URL helper using call-recordings bucket"
      contains: "call-recordings"
    - path: "services/ai-service/src/v2/call-pipeline/repository.ts"
      provides: "AI pipeline repository using call-recordings bucket"
      contains: "call-recordings"
    - path: "services/call-service/src/v2/artifact-repository.ts"
      provides: "Artifact repository using call-recordings bucket"
      contains: "call-recordings"
    - path: "infra/k8s/video-service/deployment.yaml"
      provides: "K8s deployment with call-recordings env var"
      contains: "call-recordings"
  key_links:
    - from: "services/video-service/src/index.ts"
      to: "infra/k8s/video-service/deployment.yaml"
      via: "SUPABASE_S3_BUCKET env var"
      pattern: "call-recordings"
---

<objective>
Rename all interview-recordings bucket references to call-recordings across services and infrastructure.

Purpose: The codebase still references the legacy `interview-recordings` Supabase Storage bucket name in 6 files + 1 env var. The `call-recordings` bucket already exists in Supabase Storage. All code references should point to the new bucket name. Per CONTEXT.md, the old bucket is NOT dropped (left for manual ops cleanup).

Output: All code, env vars, and K8s manifests reference `call-recordings` bucket.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-interview-migration-cleanup/48-RESEARCH.md
@services/video-service/src/v2/shared/signed-url-helper.ts
@services/video-service/src/index.ts
@services/ai-service/src/v2/call-pipeline/repository.ts
@services/call-service/src/v2/artifact-repository.ts
@infra/k8s/video-service/deployment.yaml
@.env
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename bucket references in backend services</name>
  <files>
    services/video-service/src/v2/shared/signed-url-helper.ts
    services/video-service/src/index.ts
    services/ai-service/src/v2/call-pipeline/repository.ts
    services/call-service/src/v2/artifact-repository.ts
  </files>
  <action>
    Replace `interview-recordings` with `call-recordings` in these 4 files:

    1. `services/video-service/src/v2/shared/signed-url-helper.ts` line 3:
       `const BUCKET = 'interview-recordings'` -> `const BUCKET = 'call-recordings'`

    2. `services/video-service/src/index.ts` line 132:
       `const supabaseS3Bucket = process.env.SUPABASE_S3_BUCKET || 'interview-recordings'` -> `const supabaseS3Bucket = process.env.SUPABASE_S3_BUCKET || 'call-recordings'`

    3. `services/ai-service/src/v2/call-pipeline/repository.ts` line 10:
       `const RECORDING_BUCKET = 'interview-recordings'` -> `const RECORDING_BUCKET = 'call-recordings'`

    4. `services/call-service/src/v2/artifact-repository.ts`:
       - Line 157: `.from('interview-recordings')` -> `.from('call-recordings')`
       - Line 176: `const bucketIndex = pathParts.indexOf('interview-recordings')` -> `const bucketIndex = pathParts.indexOf('call-recordings')`

    These are simple string replacements. No logic changes.
  </action>
  <verify>
    Run: `grep -rn "interview-recordings" services/video-service/src/ services/ai-service/src/ services/call-service/src/` — should return zero matches.
    Run: `grep -rn "call-recordings" services/video-service/src/ services/ai-service/src/ services/call-service/src/` — should show the 5 updated references.
    Run: `pnpm --filter @splits-network/video-service build && pnpm --filter @splits-network/ai-service build && pnpm --filter @splits-network/call-service build` — all should pass.
  </verify>
  <done>All 5 backend bucket references updated from interview-recordings to call-recordings. All three services build.</done>
</task>

<task type="auto">
  <name>Task 2: Update K8s manifest and env file bucket references</name>
  <files>
    infra/k8s/video-service/deployment.yaml
    .env
  </files>
  <action>
    1. `infra/k8s/video-service/deployment.yaml` line 80:
       `value: "interview-recordings"` -> `value: "call-recordings"`

    2. `.env` line 76:
       `SUPABASE_S3_BUCKET=interview-recordings` -> `SUPABASE_S3_BUCKET=call-recordings`

    These are the infrastructure-level references that feed the env var consumed by video-service.
  </action>
  <verify>
    Run: `grep -rn "interview-recordings" infra/ .env` — should return zero matches.
    Run: `grep -rn "call-recordings" infra/k8s/video-service/deployment.yaml .env` — should show updated values.
  </verify>
  <done>K8s deployment and .env file both reference call-recordings bucket. No interview-recordings references remain in infrastructure.</done>
</task>

</tasks>

<verification>
- `grep -rn "interview-recordings" services/ infra/ .env` returns zero matches across entire codebase
- All three backend services build successfully
- K8s manifest and .env both use call-recordings
</verification>

<success_criteria>
1. Zero references to `interview-recordings` in any service, K8s manifest, or env file
2. All references now use `call-recordings`
3. All affected services build successfully
</success_criteria>

<output>
After completion, create `.planning/phases/48-interview-migration-cleanup/48-03-SUMMARY.md`
</output>
