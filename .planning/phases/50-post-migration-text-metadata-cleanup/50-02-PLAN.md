---
phase: 50-post-migration-text-metadata-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-video/src/components/recording-consent.tsx
  - apps/portal/src/app/portal/integrations/components/calendar-preferences-panel.tsx
autonomous: true

must_haves:
  truths:
    - "Recording consent dialog says 'This Call Will Be Recorded' not 'Interview'"
    - "Recording consent body text references 'call' not 'interview'"
    - "Calendar preferences UI text says 'calls' not 'interviews'"
  artifacts:
    - path: "packages/shared-video/src/components/recording-consent.tsx"
      provides: "Recording consent UI"
      contains: "This Call Will Be Recorded"
    - path: "apps/portal/src/app/portal/integrations/components/calendar-preferences-panel.tsx"
      provides: "Calendar preferences panel"
      contains: "scheduling calls"
  key_links:
    - from: "packages/shared-video/src/components/recording-consent.tsx"
      to: "apps/video/"
      via: "shared component import"
      pattern: "This Call Will Be Recorded"
---

<objective>
Fix all user-facing "interview" text in shared-video components and portal UI where "interview" should now say "call".

Purpose: Users see "call" terminology throughout the platform, not legacy "interview" references, since the video system now supports all call types.
Output: Updated recording consent dialog and calendar preferences text.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/shared-video/src/components/recording-consent.tsx
@apps/portal/src/app/portal/integrations/components/calendar-preferences-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix recording consent text</name>
  <files>packages/shared-video/src/components/recording-consent.tsx</files>
  <action>
    Update recording-consent.tsx with these exact text changes (per user decisions):

    Line 15 heading: Change "This Interview Will Be Recorded" to "This Call Will Be Recorded"

    Line 18 body: Change "This interview will be recorded, transcribed, and summarized using AI." to "This call will be recorded, transcribed, and summarized using AI." (keep the second sentence "By joining, you consent..." unchanged)

    Lines 22-23 access note: Change "The recording will be available to the interviewer and company administrators." to "The recording will be available to call participants and administrators."

    Also grep the entire `packages/shared-video/src/` directory for any other "interview" string references. Based on the audit, recording-consent.tsx is the only file with interview text in shared-video, but confirm no others exist.
  </action>
  <verify>
    Run: `grep -rn "interview" packages/shared-video/src/` — should return NO matches (case-insensitive: `grep -rni "interview" packages/shared-video/src/`).
  </verify>
  <done>Recording consent heading says "This Call Will Be Recorded", body says "This call will be recorded...", access note references "call participants" not "interviewer".</done>
</task>

<task type="auto">
  <name>Task 2: Fix calendar preferences text and sweep portal for stale interview text</name>
  <files>apps/portal/src/app/portal/integrations/components/calendar-preferences-panel.tsx</files>
  <action>
    In calendar-preferences-panel.tsx line 311: Change "for availability when scheduling interviews." to "for availability when scheduling calls."

    NOTE: The API endpoint paths on lines 121 and 145 (`/interviews/calendar-preferences`) are backend route paths — do NOT change these as that would break the API contract. Only change the user-facing descriptive text.

    Also do a final sweep of the portal for "interview" references that should be "call". The following are EXCLUDED (legitimate domain concepts that stay as-is):
    - Application pipeline stage "interview" (badge-styles.ts, color-styles.ts, icon-styles.ts, accent.ts, pipeline-modal.tsx)
    - Application stage notifications (notification-service templates referencing interview stage)
    - Call type label `interview: "Interview"` in calls/types.ts (a call can be of type "interview")
    - Landing/marketing page content (page.tsx "Screen, interview, offer", for-companies content)
    - Privacy policy text
    - Documentation README
    - Permission configurator text about "screening, interviews" stages
    - Dashboard views referencing interview stats (recruiter-view.tsx, company-view.tsx — these track the interview stage in application pipeline)
    - Calendar create-event-modal.tsx (manages application interview stage promotion)
    - Application search badge for "interview" stage
    - ATS integration `sync_interviews` field (external system integration)
    - Notification service monthly report `applicationsInterviewing` counter
    - Notification service application stage change handlers for "interview" stage
    - Notification email templates about interview scheduling (application stage context)

    If any NON-excluded "interview" references are found during sweep, fix them. Use judgment: if "interview" refers to the application pipeline stage concept, leave it. If it refers to a video call/session, change to "call".
  </action>
  <verify>
    Run: `grep -rni "interview" packages/shared-video/src/` — should return NO matches.
    Run: `grep -n "interview" apps/portal/src/app/portal/integrations/components/calendar-preferences-panel.tsx` — should return only the API endpoint paths (lines with `/interviews/`), NOT any user-facing text.
  </verify>
  <done>Calendar preferences says "scheduling calls". Recording consent fully updated. No stale "interview" text remains in shared-video. All remaining portal "interview" references are legitimate domain concepts (application pipeline stage).</done>
</task>

</tasks>

<verification>
1. `grep -rni "interview" packages/shared-video/src/` returns zero matches
2. Recording consent component renders "This Call Will Be Recorded" heading
3. Calendar preferences text says "scheduling calls" not "scheduling interviews"
4. All remaining "interview" references in portal are legitimate domain concepts (application stage)
</verification>

<success_criteria>
- Recording consent dialog fully updated to "call" terminology
- Calendar preferences UI text updated
- No stale interview references in shared-video package
</success_criteria>

<output>
After completion, create `.planning/phases/50-post-migration-text-metadata-cleanup/50-02-SUMMARY.md`
</output>
