---
phase: 50-post-migration-text-metadata-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/notification-service/src/services/calls/service.ts
  - services/notification-service/src/services/calls/in-app-service.ts
  - services/notification-service/src/consumers/calls/consumer.ts
  - services/video-service/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Notification metadata eventType for recording ready uses underscore format matching RabbitMQ routing key"
    - "Video-service Swagger description says 'video call service' not 'interview'"
    - "Error log messages reference correct event key format for debugging"
  artifacts:
    - path: "services/notification-service/src/services/calls/service.ts"
      provides: "Email notification metadata"
      contains: "call.recording_ready"
    - path: "services/notification-service/src/services/calls/in-app-service.ts"
      provides: "In-app notification metadata"
      contains: "call.recording_ready"
    - path: "services/notification-service/src/consumers/calls/consumer.ts"
      provides: "Event consumer error logging"
      contains: "call.recording_ready"
    - path: "services/video-service/src/index.ts"
      provides: "Swagger description"
      contains: "video call service"
  key_links:
    - from: "services/video-service/src/v2/calls/call-recording-webhook.ts"
      to: "services/notification-service/src/services/calls/service.ts"
      via: "RabbitMQ event routing key -> metadata eventType"
      pattern: "call\\.recording_ready"
---

<objective>
Fix notification metadata keys and service Swagger descriptions to eliminate interview-era mismatches.

Purpose: Notification metadata eventType values must match RabbitMQ routing key format for debugging consistency. Swagger descriptions must reflect the current "call" terminology.
Output: Corrected metadata keys and Swagger text across notification-service and video-service.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@services/notification-service/src/services/calls/service.ts
@services/notification-service/src/services/calls/in-app-service.ts
@services/notification-service/src/consumers/calls/consumer.ts
@services/video-service/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix notification metadata eventType keys and error log</name>
  <files>
    services/notification-service/src/services/calls/service.ts
    services/notification-service/src/services/calls/in-app-service.ts
    services/notification-service/src/consumers/calls/consumer.ts
  </files>
  <action>
    In service.ts (~line 239): Change `eventType: 'call.recording.ready'` to `eventType: 'call.recording_ready'`

    In in-app-service.ts (~line 162): Change `eventType: 'call.recording.ready'` to `eventType: 'call.recording_ready'`

    In consumer.ts (~line 260): Change error log message from `'Failed to process call.recording.ready'` to `'Failed to process call.recording_ready'`

    Also audit ALL other eventType values in both service.ts and in-app-service.ts for any dot-vs-underscore inconsistencies. The existing ones (`call.created`, `call.cancelled`, `call.rescheduled`, `call.reminder`, `call.starting_soon`, `call.instant`, `call.participant.joined`, `call.declined`) use dots as segment separators which is correct — the issue is specifically `recording.ready` having an extra dot where it should be `recording_ready` (compound word, not segment).
  </action>
  <verify>
    Run: `grep -rn "call\.recording\.ready" services/notification-service/src/` — should return NO matches.
    Run: `grep -rn "call\.recording_ready" services/notification-service/src/services/calls/` — should return 2 matches (service.ts and in-app-service.ts).
    Run: `grep -rn "call\.recording_ready" services/notification-service/src/consumers/calls/consumer.ts` — should return 1 match in the error log.
  </verify>
  <done>All notification-service eventType metadata and error logs use `call.recording_ready` (underscore) matching the RabbitMQ routing key format.</done>
</task>

<task type="auto">
  <name>Task 2: Fix video-service Swagger description</name>
  <files>services/video-service/src/index.ts</files>
  <action>
    Change line 76 from:
    `description: "Internal video interview service for Splits Network"`
    to:
    `description: "Internal video call service for Splits Network"`

    Also scan all other service index.ts files for any Swagger descriptions containing "interview". Based on the audit, only video-service has this issue — the others use correct descriptions.
  </action>
  <verify>
    Run: `grep -rn "interview" services/video-service/src/index.ts` — should return NO matches.
    Run: `grep -rn "interview" services/*/src/index.ts` — should return NO matches.
  </verify>
  <done>Video-service Swagger description says "Internal video call service for Splits Network". No other service Swagger descriptions reference "interview".</done>
</task>

</tasks>

<verification>
1. `grep -rn "call\.recording\.ready" services/` returns zero matches (dot format fully eliminated)
2. `grep -rn "interview" services/video-service/src/index.ts` returns zero matches
3. `grep -rn "interview" services/*/src/index.ts` returns zero matches (no Swagger description references)
</verification>

<success_criteria>
- Notification metadata eventType for recording_ready uses underscore format everywhere
- Video-service Swagger says "video call service"
- Error log messages match actual routing key format
</success_criteria>

<output>
After completion, create `.planning/phases/50-post-migration-text-metadata-cleanup/50-01-SUMMARY.md`
</output>
