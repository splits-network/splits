---
phase: 46-interview-migration
plan: 04
type: execute
wave: 2
depends_on: ["46-03"]
files_modified:
  - apps/portal/src/app/portal/applications/components/shared/application-detail-panel.tsx
  - apps/portal/src/app/portal/applications/components/shared/application-detail-header.tsx
  - apps/portal/src/app/portal/applications/components/shared/actions-toolbar.tsx
autonomous: true

must_haves:
  truths:
    - "Application detail panel has a 'Calls' tab (not 'Interviews') showing entity-linked calls"
    - "Application detail header has a 'Schedule Call' shortcut button"
    - "'Schedule Call' from application context opens call creation modal pre-filled with application entity and candidate participant"
    - "No remaining references to interview pages, components, or hooks in portal app"
  artifacts:
    - path: "apps/portal/src/app/portal/applications/components/shared/application-detail-panel.tsx"
      provides: "Calls tab replacing Interviews tab with entity-scoped call list"
  key_links:
    - from: "apps/portal/src/app/portal/applications/components/shared/application-detail-panel.tsx"
      to: "apps/portal/src/app/portal/calls/components/"
      via: "Entity-scoped call tab component"
      pattern: "entity_type.*application"
    - from: "apps/portal/src/app/portal/applications/components/shared/application-detail-header.tsx"
      to: "apps/portal/src/components/calls/call-creation-modal.tsx"
      via: "Schedule Call button opens modal"
      pattern: "CallCreation|call-creation"
---

<objective>
Replace the application "Interviews" tab with a "Calls" tab using entity-scoped call tabs (Phase 44-09 pattern), and add a "Schedule Call" shortcut button to the application detail header.

Purpose: Applications now link to calls instead of interviews. Users need a quick way to schedule interview-type calls from the application context, and to see all calls linked to the application.
Output: Application detail panel with working Calls tab and Schedule Call shortcut.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-interview-migration/46-CONTEXT.md

@apps/portal/src/app/portal/applications/components/shared/application-detail-panel.tsx
@apps/portal/src/app/portal/applications/components/shared/application-detail-header.tsx
@apps/portal/src/app/portal/applications/components/shared/actions-toolbar.tsx
@apps/portal/src/app/portal/roles/components/shared/job-calls-tab.tsx
@apps/portal/src/app/portal/companies/components/shared/company-calls-tab.tsx
@apps/portal/src/components/calls/call-creation-modal.tsx
@apps/portal/src/hooks/use-create-call.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Interviews tab with Calls tab on application detail</name>
  <files>
    apps/portal/src/app/portal/applications/components/shared/application-detail-panel.tsx
  </files>
  <action>
Update `application-detail-panel.tsx` to replace the "Interviews" tab with a "Calls" tab:

1. Remove the import of `ApplicationInterviewsTab` (already deleted in Plan 03)

2. In the TABS array, change the "interviews" entry:
   - `key: "calls"` (was "interviews")
   - `label: "Calls"` (was "Interviews")
   - `icon: "fa-phone"` (was "fa-video")

3. Update the TabKey type to use `"calls"` instead of `"interviews"`

4. For the tab content rendering, follow the EXACT pattern used by `job-calls-tab.tsx` and `company-calls-tab.tsx` (Phase 44-09). Read those files to understand the entity-scoped call tab pattern. The application calls tab should:
   - Pass `entity_type="application"` and `entity_id={application.id}`
   - Use the same entity-scoped call list component pattern
   - If job-calls-tab and company-calls-tab use a shared component, reuse it. If they inline the logic, create an inline version for applications following the same pattern.
   - Use `syncToUrl: false` per Phase 44-09 decision
   - Use `limit: 10` default per Phase 44-09 decision

5. If the entity call tab is a shared component (check `apps/portal/src/app/portal/calls/components/shared/`), just import and use it. If each entity type has its own tab file, create `apps/portal/src/app/portal/applications/components/shared/application-calls-tab.tsx` following the pattern from job-calls-tab.tsx but with entity_type="application".
  </action>
  <verify>Run `pnpm --filter @splits-network/portal build` to verify the portal compiles.</verify>
  <done>Application detail panel shows "Calls" tab that lists entity-linked calls for the application</done>
</task>

<task type="auto">
  <name>Task 2: Add Schedule Call shortcut to application detail header</name>
  <files>
    apps/portal/src/app/portal/applications/components/shared/application-detail-header.tsx
    apps/portal/src/app/portal/applications/components/shared/actions-toolbar.tsx
  </files>
  <action>
Add a "Schedule Call" button to the application detail that opens the call creation modal pre-filled with application context.

1. Read `apps/portal/src/components/calls/call-creation-modal.tsx` to understand its props interface. Identify what props it accepts for pre-filling:
   - Entity type and entity ID (to auto-link the call to this application)
   - Participant pre-fill (to auto-add the candidate)
   - Call type pre-fill (to default to "interview" type)

2. Read `apps/portal/src/hooks/use-create-call.ts` to understand how other parts of the app trigger call creation.

3. Read how `apps/portal/src/components/portal-chat-sidebar.tsx` triggers call creation (Phase 44-11 pattern) for reference on how to open the modal with pre-filled data.

4. Add to `application-detail-header.tsx` (or `actions-toolbar.tsx` -- whichever is more appropriate for action buttons):
   - A "Schedule Call" button with icon `fa-phone`
   - Use DaisyUI `btn btn-sm btn-primary` styling
   - On click, either:
     a. Open the CallCreationModal inline with pre-filled props (if the modal accepts props for entity/participant pre-fill), OR
     b. Navigate to the calls page with URL params for pre-fill (if that's the established pattern from Phase 44-11's "Schedule Follow-up" feature which uses URL params)

   The Phase 44-11 decision says "Schedule Follow-up uses URL params to pre-fill portal call creation page". Follow this same pattern:
   - Navigate to `/portal/calls?create=true&entity_type=application&entity_id={application.id}&participant={candidate.user_id}&call_type=interview`
   - The call creation page/modal should pick up these URL params

5. Per context decisions:
   - **Auto-link to application entity** when creating from application detail context
   - **Auto-add candidate** as participant when creating from application context
   - The candidate's user_id should come from `application.candidate.user_id` or similar field

6. Verify the actions-toolbar doesn't still have any interview-specific buttons (schedule interview, etc.). If it does, remove them. Check for imports of the deleted schedule-interview-modal and remove them.

7. Check that no files import from the deleted scheduling modals. The `application-search.tsx`, `available-slots-list.tsx`, and `platform-selector.tsx` in the scheduling directory may or may not still be needed -- if they're only imported by deleted interview modals and not by the call creation modal, they can be deleted in this task.
  </action>
  <verify>
Run `pnpm --filter @splits-network/portal build` to verify the portal compiles. Then do a final grep for any remaining "interview" references in the portal app that represent real code (not UX labels or content strings): `grep -r "interview" apps/portal/src --include="*.ts" --include="*.tsx" -l` -- review each hit to confirm it's either a UX label (acceptable) or content/documentation (acceptable), not dead code or broken imports.
  </verify>
  <done>"Schedule Call" shortcut on application detail opens call creation with application entity and candidate pre-filled; no dead interview code remains in portal</done>
</task>

</tasks>

<verification>
- Application detail panel has "Calls" tab showing entity-linked calls
- "Schedule Call" button exists on application detail header or toolbar
- Clicking "Schedule Call" opens call creation with application entity pre-linked and candidate pre-filled
- Portal builds successfully with zero broken imports
- Remaining "interview" mentions in portal are only UX labels (stage names, call type badges) per context decision
</verification>

<success_criteria>
Application detail fully migrated from interview system to call system. Users can view application-linked calls and schedule new interview-type calls from the application detail page. v10.0 milestone complete.
</success_criteria>

<output>
After completion, create `.planning/phases/46-interview-migration/46-04-SUMMARY.md`
</output>
