---
phase: 46-interview-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/video-service/src/v2/routes.ts
  - services/video-service/src/v2/interviews/recording-webhook.ts
  - services/api-gateway/src/routes/v2/video.ts
  - services/api-gateway/src/routes/v2/routes.ts
  - services/ai-service/src/domain-consumer.ts
  - services/notification-service/src/domain-consumer.ts
  - services/notification-service/src/index.ts
  - services/notification-service/src/service.ts
  - services/notification-service/src/templates/index.ts
  - services/integration-service/src/v2/calendar/service.ts
autonomous: true

must_haves:
  truths:
    - "video-service interviews directory is deleted except recording-webhook which is refactored to call-only"
    - "ai-service no longer binds to or handles interview.recording_ready events"
    - "ai-service transcription directory (interview-specific pipeline) is deleted"
    - "Gateway no longer proxies /api/v2/interviews routes"
    - "notification-service interview consumer, templates, jobs, and service are deleted"
    - "All services build successfully after cleanup"
  artifacts:
    - path: "services/video-service/src/v2/routes.ts"
      provides: "Simplified route registration without interview routes"
    - path: "services/ai-service/src/domain-consumer.ts"
      provides: "Consumer without interview.recording_ready binding"
  key_links:
    - from: "services/video-service/src/v2/routes.ts"
      to: "services/video-service/src/v2/calls/"
      via: "call recording routes still registered"
      pattern: "registerCallRecordingRoutes"
    - from: "services/ai-service/src/domain-consumer.ts"
      to: "services/ai-service/src/v2/call-pipeline/"
      via: "call.recording_ready still handled"
      pattern: "call\\.recording_ready"
---

<objective>
Remove all interview-specific backend code from video-service, ai-service, api-gateway, notification-service, and integration-service.

Purpose: The call system fully replaces the interview system. All interview-specific service code, routes, consumers, templates, and jobs must be deleted. The generalized call pipeline (Phase 45) handles everything now.
Output: Clean backend services with no interview-specific code paths.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-interview-migration/46-CONTEXT.md

@services/video-service/src/v2/routes.ts
@services/video-service/src/v2/interviews/recording-webhook.ts
@services/api-gateway/src/routes/v2/video.ts
@services/api-gateway/src/routes/v2/routes.ts
@services/ai-service/src/domain-consumer.ts
@services/notification-service/src/domain-consumer.ts
@services/notification-service/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove interview code from video-service and api-gateway</name>
  <files>
    services/video-service/src/v2/interviews/ (DELETE entire directory)
    services/video-service/src/v2/routes.ts
    services/api-gateway/src/routes/v2/video.ts (DELETE file)
    services/api-gateway/src/routes/v2/routes.ts
  </files>
  <action>
**video-service cleanup:**

1. DELETE the entire `services/video-service/src/v2/interviews/` directory (10 files: recording-routes.ts, recording-service.ts, recording-webhook.ts, repository.ts, routes.ts, scheduling-service.ts, service.ts, signed-url-helper.ts, token-service.ts, types.ts)

2. Rewrite `services/video-service/src/v2/routes.ts` to:
   - Remove ALL imports from `./interviews/*` (registerInterviewRoutes, registerRecordingRoutes, registerRecordingWebhook, RecordingService, InterviewRepository, TokenService)
   - Keep call recording imports (CallRecordingRepository, CallRecordingService, registerCallRecordingRoutes)
   - Remove interview repository/service instantiation
   - Remove interview route registration (registerInterviewRoutes, registerRecordingRoutes)
   - IMPORTANT: The recording webhook (`/api/v2/interviews/recording/webhook`) currently handles BOTH interview and call egress events (unified webhook from Phase 45). The call recording has its own webhook at `/api/v2/calls/recording/webhook`. After removing the interview webhook, verify that call recordings still have a working webhook path. The call recording routes file (`call-recording-routes.ts`) should already handle this. If the unified webhook was the ONLY webhook endpoint, create a new call-only webhook in `services/video-service/src/v2/calls/call-recording-webhook.ts` that registers at `/api/v2/calls/recording/webhook` (or verify this already exists in call-recording-routes.ts).
   - Remove RecordingService/InterviewRepository from RegisterConfig interface
   - Keep s3Config, callRecordingRepository, callRecordingService instantiation
   - Keep registerCallRecordingRoutes call

3. Check `services/video-service/src/v2/calls/call-recording-routes.ts` to verify it has its own webhook endpoint. If it does NOT have a webhook for call egress events, extract the call-related webhook logic from the deleted recording-webhook.ts into a new `call-recording-webhook.ts` in the calls directory, registering at a call-specific path. The gateway already has a route for `/api/v2/calls/recording/webhook` (from Phase 45-03).

**api-gateway cleanup:**

4. DELETE `services/api-gateway/src/routes/v2/video.ts` entirely (this file only proxies `/api/v2/interviews` and `/api/v2/interviews/*` to video-service -- no longer needed)

5. Update `services/api-gateway/src/routes/v2/routes.ts`:
   - Remove the import of `registerVideoRoutes` from `./video`
   - Remove the `registerVideoRoutes(app, services)` call
   - Keep all other route registrations intact
  </action>
  <verify>
Run `pnpm --filter @splits-network/video-service build` and `pnpm --filter @splits-network/api-gateway build` to verify both services compile without errors.
  </verify>
  <done>video-service has no interview code; api-gateway no longer proxies interview routes; call recording webhook path still works</done>
</task>

<task type="auto">
  <name>Task 2: Remove interview code from ai-service, notification-service, and integration-service</name>
  <files>
    services/ai-service/src/domain-consumer.ts
    services/ai-service/src/v2/transcription/ (DELETE entire directory)
    services/ai-service/tests/unit/domain-consumer.test.ts
    services/notification-service/src/domain-consumer.ts
    services/notification-service/src/index.ts
    services/notification-service/src/service.ts
    services/notification-service/src/consumers/interviews/ (DELETE entire directory)
    services/notification-service/src/services/interviews/ (DELETE entire directory)
    services/notification-service/src/templates/interviews/ (DELETE entire directory)
    services/notification-service/src/jobs/send-interview-reminders.ts (DELETE)
    services/integration-service/src/v2/calendar/service.ts
  </files>
  <action>
**ai-service cleanup:**

1. DELETE the entire `services/ai-service/src/v2/transcription/` directory (3 files: service.ts, summarizer.ts, repository.ts). This is the interview-specific transcription pipeline. The generalized `call-pipeline/` directory (Phase 45) handles all call types now.

2. Update `services/ai-service/src/domain-consumer.ts`:
   - Remove import of `TranscriptionPipelineService`
   - Remove `transcriptionPipeline` from constructor parameters
   - Remove `interview.recording_ready` from queue binding (`bindQueue` call)
   - Remove `interview.recording_ready` from the bindings log array
   - Remove the `case 'interview.recording_ready':` handler and `handleRecordingReady` method entirely
   - Keep `call.recording_ready` binding and handler intact

3. Update `services/ai-service/tests/unit/domain-consumer.test.ts` -- remove any test cases that reference interview.recording_ready or TranscriptionPipelineService. If the file only tests interview-related events, delete it.

4. Check the ai-service index.ts to see where TranscriptionPipelineService is instantiated and passed to DomainEventConsumer -- remove that instantiation and its dependencies.

**notification-service cleanup:**

5. DELETE `services/notification-service/src/consumers/interviews/` directory (consumer.ts)
6. DELETE `services/notification-service/src/services/interviews/` directory (service.ts)
7. DELETE `services/notification-service/src/templates/interviews/` directory (index.ts, reminder-emails.ts, candidate-emails.ts, interviewer-emails.ts)
8. DELETE `services/notification-service/src/jobs/send-interview-reminders.ts`

9. Update `services/notification-service/src/domain-consumer.ts`:
   - Remove import of `InterviewsEventConsumer`
   - Remove `interviewsConsumer` private field
   - Remove `interviewsConsumer` instantiation in constructor
   - Remove all `interview.*` event case handlers (interview.created, interview.cancelled, interview.rescheduled, interview.reschedule_requested, interview.reschedule_accepted)
   - Remove `interview.*` queue bindings if they exist
   - Keep all other consumers intact

10. Update `services/notification-service/src/index.ts`:
    - Remove import of `sendInterviewReminders`
    - Remove the cron job that calls `sendInterviewReminders`
    - Keep all other cron jobs and initialization

11. Update `services/notification-service/src/service.ts` -- remove any interview-related service property or method if it has one (check the file).

12. Update `services/notification-service/src/templates/index.ts` -- remove any re-exports from the deleted interviews template directory.

**integration-service cleanup:**

13. Update `services/integration-service/src/v2/calendar/service.ts` -- update the comment "Create a calendar event (interview scheduling)" to "Create a calendar event (call scheduling)" -- this is just a comment fix.
  </action>
  <verify>
Run builds for all three services:
- `pnpm --filter @splits-network/ai-service build`
- `pnpm --filter @splits-network/notification-service build`
- `pnpm --filter @splits-network/integration-service build`
All must compile without errors.
  </verify>
  <done>ai-service, notification-service, and integration-service have no interview-specific code; all three build successfully</done>
</task>

</tasks>

<verification>
- `services/video-service/src/v2/interviews/` directory no longer exists
- `services/ai-service/src/v2/transcription/` directory no longer exists
- `services/notification-service/src/consumers/interviews/` directory no longer exists
- `services/notification-service/src/services/interviews/` directory no longer exists
- `services/notification-service/src/templates/interviews/` directory no longer exists
- `services/api-gateway/src/routes/v2/video.ts` no longer exists
- All five services build successfully: video-service, api-gateway, ai-service, notification-service, integration-service
</verification>

<success_criteria>
All backend services compile cleanly with zero interview-specific code paths. The call system (call-service, call recording pipeline, call notifications) continues to function through its own code paths.
</success_criteria>

<output>
After completion, create `.planning/phases/46-interview-migration/46-02-SUMMARY.md`
</output>
