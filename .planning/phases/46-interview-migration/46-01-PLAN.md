---
phase: 46-interview-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260313000001_drop_interview_tables.sql
autonomous: true

must_haves:
  truths:
    - "All interview-related database tables are dropped"
    - "interview_id column is removed from applications table"
    - "Interview-related enums are dropped"
    - "application_notes note_type constraint no longer includes interview_summary or interview_note"
  artifacts:
    - path: "supabase/migrations/20260313000001_drop_interview_tables.sql"
      provides: "Complete interview schema removal migration"
      contains: "DROP TABLE"
  key_links: []
---

<objective>
Drop all interview-related database tables, enums, and columns via a single Supabase migration.

Purpose: Remove the entire interview schema now that the call system fully replaces it. No production data exists in these tables, so this is purely schema cleanup.
Output: One migration SQL file that removes all interview database objects.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-interview-migration/46-CONTEXT.md

@supabase/migrations/20260307000005_create_interviews_schema.sql
@supabase/migrations/20260308000001_extend_interviews_for_scheduling.sql
@supabase/migrations/20260309000001_add_recording_columns.sql
@supabase/migrations/20260310000001_add_interview_transcripts.sql
@supabase/migrations/20260311000001_extend_interviews_for_panel_notes.sql
@supabase/migrations/20260312000002_add_call_id_to_interviews.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration to drop all interview database objects</name>
  <files>supabase/migrations/20260313000001_drop_interview_tables.sql</files>
  <action>
Create a single migration file that drops ALL interview-related database objects in the correct dependency order. Use DROP CASCADE where FK dependencies exist.

Tables to drop (order matters -- drop dependent tables first):
1. `interview_notes` (FK to interview_participants, interviews)
2. `interview_recording_consents` (FK to interview_participants, interviews)
3. `interview_access_tokens` (FK to interview_participants, interviews)
4. `interview_reschedule_requests` (FK to interviews)
5. `interview_transcripts` (FK to interviews)
6. `interview_participants` (FK to interviews)
7. `interviews` (FK from applications via call_id -- but call_id references calls, not interviews)

Before dropping `interviews`, remove the `call_id` column and its unique partial index from `interviews` (added in 20260312000002). Actually, since we're dropping the whole table, CASCADE handles this.

Also handle:
- Drop the `interview_id` column from `applications` table if it exists (check -- the context decisions say "Remove interview_id column from applications table"). Search migrations for this column. NOTE: Looking at the migrations, `interviews` has `application_id` referencing `applications`, not the other way around. But the CONTEXT says "Remove interview_id column from applications table" -- this may not exist as a column on applications. If it doesn't exist, skip it. Add a comment noting this.
- Drop triggers: `set_interviews_updated_at`, `set_interview_notes_updated_at`, `set_interview_reschedule_requests_updated_at`
- Drop enums: `interview_status`, `interview_type`, `interview_participant_role`
- Clean the `application_notes` `note_type` CHECK constraint -- remove `interview_summary` and `interview_note` values. Rebuild the constraint with remaining values only.
- Drop indexes that won't be auto-dropped by table drops (most will cascade)

Use `DROP TABLE IF EXISTS ... CASCADE` for safety. Use `DROP TYPE IF EXISTS ... CASCADE` for enums.

Add clear section comments for each group of operations. Keep the migration idempotent with IF EXISTS guards.

Do NOT touch `user_calendar_preferences` table -- it was created in the interview scheduling migration but is still used by the call calendar system.
  </action>
  <verify>
Review the migration SQL for:
1. All 7 interview tables listed for drop
2. All 3 interview enums listed for drop
3. application_notes constraint rebuilt without interview values
4. user_calendar_preferences NOT dropped
5. Correct dependency order (or CASCADE usage)
  </verify>
  <done>Migration file exists that drops all interview database objects while preserving call-system and calendar tables</done>
</task>

</tasks>

<verification>
- Migration file exists at `supabase/migrations/20260313000001_drop_interview_tables.sql`
- File contains DROP statements for all 7 interview tables
- File contains DROP TYPE for all 3 interview enums
- File rebuilds application_notes note_type constraint without interview values
- File does NOT drop user_calendar_preferences, calls, call_participants, or any call-system tables
</verification>

<success_criteria>
Single migration file ready to apply that completely removes the interview database schema while preserving the call system and shared calendar infrastructure.
</success_criteria>

<output>
After completion, create `.planning/phases/46-interview-migration/46-01-SUMMARY.md`
</output>
