---
phase: 34-video-call-experience
plan: "03"
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - packages/shared-video/src/components/video-room.tsx
  - packages/shared-video/src/components/video-controls.tsx
  - packages/shared-video/src/components/participant-tile.tsx
  - packages/shared-video/src/components/self-view-pip.tsx
  - packages/shared-video/src/components/connection-quality.tsx
  - packages/shared-video/src/components/post-call-summary.tsx
  - packages/shared-video/src/index.ts
autonomous: true

must_haves:
  truths:
    - "User sees remote participant as large view with self as small PiP corner"
    - "User can mute/unmute mic, toggle camera, and leave the call from controls bar"
    - "User sees connection quality signal bars on video tiles"
    - "Camera-off shows avatar instead of blank video"
    - "Post-call screen shows call duration and close/redirect option"
  artifacts:
    - path: "packages/shared-video/src/components/video-room.tsx"
      provides: "In-call room layout with active speaker + PiP"
      min_lines: 60
    - path: "packages/shared-video/src/components/video-controls.tsx"
      provides: "Always-visible controls bar"
      min_lines: 40
    - path: "packages/shared-video/src/components/post-call-summary.tsx"
      provides: "Post-call duration and redirect screen"
      min_lines: 30
  key_links:
    - from: "packages/shared-video/src/components/video-room.tsx"
      to: "useTracks, useRemoteParticipants, useLocalParticipant"
      via: "import from @livekit/components-react"
      pattern: "useTracks|useRemoteParticipants"
    - from: "packages/shared-video/src/components/video-room.tsx"
      to: "RoomAudioRenderer"
      via: "import from @livekit/components-react"
      pattern: "RoomAudioRenderer"
---

<objective>
Build the in-call room components: active speaker layout with PiP self-view, always-visible controls bar, connection quality indicator, and post-call summary screen. These components render INSIDE a LiveKitRoom context.

Purpose: The core video call experience during the interview. Must feel professional and stable.
Output: Room component set exported from shared-video package.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-video-call-experience/34-CONTEXT.md
@.planning/phases/34-video-call-experience/34-RESEARCH.md
@.planning/phases/34-video-call-experience/34-01-SUMMARY.md
@packages/shared-video/src/types.ts
@packages/shared-video/src/components/camera-off-fallback.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Participant tile, self-view PiP, and connection quality</name>
  <files>
    packages/shared-video/src/components/participant-tile.tsx
    packages/shared-video/src/components/self-view-pip.tsx
    packages/shared-video/src/components/connection-quality.tsx
  </files>
  <action>
    Create three video display components that render inside LiveKitRoom context:

    **connection-quality.tsx** — Signal bars indicator:
    - Props: `{ participant: Participant }` from livekit-client
    - Use `useConnectionQualityIndicator` hook from `@livekit/components-react` for fully custom rendering
    - Render 3 vertical bars of increasing height (4px, 8px, 12px)
    - Excellent (3 bars active): `bg-success`
    - Good (2 bars active): `bg-warning`
    - Poor (1 bar active): `bg-error`
    - Unknown: all bars `bg-base-content/20`
    - Compact layout: `flex items-end gap-0.5` — sits in corner of video tile
    - See RESEARCH.md "Connection Quality Signal Bars" for pattern

    **participant-tile.tsx** — Remote participant large view:
    - Props: `{ participant: RemoteParticipant, name: string, avatarUrl?: string }`
    - Full container (`w-full h-full`) for the large remote view
    - Use `useTracks` filtered to this participant's camera track
    - If camera track exists: render `VideoTrack` component from `@livekit/components-react` with `className="w-full h-full object-cover"`
    - If no camera track: render `CameraOffFallback` (import from sibling component)
    - Overlay in top-right corner: `ConnectionQuality` component
    - Overlay at bottom: participant name in semi-transparent bar (`bg-black/50 text-white px-4 py-2`)

    **self-view-pip.tsx** — Self-view picture-in-picture:
    - Props: `{ name: string, avatarUrl?: string }`
    - Container: `absolute bottom-20 right-4 w-48 overflow-hidden shadow-lg z-10` (bottom-20 to stay above controls bar)
    - Use `useLocalParticipant` to get local participant
    - Use `useTracks` filtered to local participant's camera
    - If camera track: render `VideoTrack` with `className="w-full aspect-video object-cover"`
    - If no camera: render small `CameraOffFallback`
    - ConnectionQuality indicator in corner
    - Mobile: `w-24` (smaller on mobile via `w-24 lg:w-48`)
  </action>
  <verify>
    All three files exist under `packages/shared-video/src/components/`.
    `tsc --noEmit` passes in shared-video.
    Grep for `useConnectionQualityIndicator` or `ConnectionQuality` in connection-quality.tsx.
    Grep for `VideoTrack` in participant-tile.tsx.
  </verify>
  <done>Remote participant tile, self-view PiP, and connection quality signal bars built. All use LiveKit React hooks for track rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Video room layout, controls bar, and post-call summary</name>
  <files>
    packages/shared-video/src/components/video-room.tsx
    packages/shared-video/src/components/video-controls.tsx
    packages/shared-video/src/components/post-call-summary.tsx
    packages/shared-video/src/index.ts
  </files>
  <action>
    Create the main in-call room component, controls bar, and post-call screen:

    **video-controls.tsx** — Always-visible controls bar:
    - Render at bottom center of the screen: `fixed bottom-0 left-0 right-0` with `bg-base-300/90 backdrop-blur-md`
    - Centered button group with generous spacing: `flex items-center justify-center gap-4 py-4`
    - Buttons (all DaisyUI `btn btn-circle`):
      - Mic toggle: `fa-microphone` / `fa-microphone-slash` — use `TrackToggle` from LiveKit with `source={Track.Source.Microphone}`. Style the TrackToggle or wrap it to use DaisyUI btn classes. When muted: `btn-error` background.
      - Camera toggle: `fa-video` / `fa-video-slash` — use `TrackToggle` with `source={Track.Source.Camera}`. When off: `btn-error` background.
      - Device settings: `fa-gear` — opens a DaisyUI `dropdown dropdown-top` with `MediaDeviceSelect` components for camera, mic, speaker
      - Leave call: `fa-phone-hangup` — use `DisconnectButton` from LiveKit, styled as `btn btn-circle btn-error btn-lg`
    - ALWAYS visible (per RESEARCH.md recommendation for professional interview context)
    - Controls bar is Claude's discretion per CONTEXT.md — decision: always visible

    **video-room.tsx** — Main in-call layout:
    - Props: `{ interviewContext: InterviewContext, localName: string, localAvatarUrl?: string, onDisconnect: () => void }`
    - This component renders INSIDE `<LiveKitRoom>` — it assumes room context exists
    - Full screen dark background: `relative h-screen bg-base-300`
    - Use `useRemoteParticipants()` to detect remote participant
    - If remote participant exists: render `ParticipantTile` (full screen) + `SelfViewPip` (corner overlay)
    - If no remote participant: render `WaitingIndicator` centered (import from lobby components), showing "Waiting for {name}..."
    - Include `<RoomAudioRenderer />` — CRITICAL for remote audio (see RESEARCH.md Pitfall 3)
    - Include `<StartAudio />` from LiveKit — handles autoplay policy in Safari (see RESEARCH.md Pitfall 4)
    - Render `<VideoControls />` at bottom
    - Get remote participant name/avatar from `interviewContext.participants` by matching against remote participant identity

    **post-call-summary.tsx** — End-of-call screen:
    - Props: `{ duration: number, isCandidate: boolean, onClose: () => void }`
    - Centered card on neutral background
    - Heading: "Interview Ended" in `text-2xl font-black`
    - Duration display: formatted MM:SS (or HH:MM:SS if > 1 hour)
    - For candidates (`isCandidate=true`): "Thank you for your time" message, "Close" button
    - For portal users (`isCandidate=false`): "Redirecting in 10 seconds..." with countdown, plus "Close Tab" button
    - Auto-close timer: `useEffect` with 10-second countdown for portal users, calls `onClose` when done
    - Clean, professional design. DaisyUI card component.

    **index.ts** — Add exports for all new room components:
    - VideoRoom, VideoControls, ParticipantTile, SelfViewPip, ConnectionQuality, PostCallSummary
  </action>
  <verify>
    `pnpm --filter @splits-network/shared-video build` or `tsc --noEmit` passes.
    Grep for `RoomAudioRenderer` in video-room.tsx confirms it's included.
    Grep for `StartAudio` in video-room.tsx confirms autoplay handling.
    Grep for `TrackToggle` in video-controls.tsx confirms LiveKit integration.
    All room components exported from index.ts.
  </verify>
  <done>In-call room with active speaker layout, always-visible controls bar (mic/camera/settings/leave), connection quality on each tile, and post-call summary screen with duration and auto-redirect.</done>
</task>

</tasks>

<verification>
- All 6 room components exist and export from shared-video
- VideoRoom includes RoomAudioRenderer and StartAudio
- Controls bar is always visible with mic, camera, settings, and leave buttons
- Connection quality shows signal bars (3 levels)
- Post-call summary shows duration and handles redirect
- Self-view PiP is responsive (w-24 mobile, w-48 desktop)
</verification>

<success_criteria>
- In-call shows large remote + small self PiP layout
- Controls bar always visible with working toggles
- Connection quality indicator on video tiles
- Camera-off shows avatar for both local and remote
- Post-call shows duration and auto-redirects for portal users
</success_criteria>

<output>
After completion, create `.planning/phases/34-video-call-experience/34-03-SUMMARY.md`
</output>
