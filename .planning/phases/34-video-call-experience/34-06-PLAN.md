---
phase: 34-video-call-experience
plan: "06"
type: execute
wave: 4
depends_on: ["34-04", "34-05"]
files_modified:
  - apps/portal/src/app/portal/interview/[id]/interview-client.tsx
  - apps/candidate/src/app/(public)/interview/[token]/interview-client.tsx
  - packages/shared-video/src/components/video-lobby.tsx
  - packages/shared-video/src/components/video-room.tsx
autonomous: false

must_haves:
  truths:
    - "Both portal and candidate apps build successfully with all video components"
    - "Interview page renders without runtime errors"
    - "All LiveKit anti-patterns are avoided (no LiveKitRoom in lobby, no remounting)"
  artifacts:
    - path: "apps/portal/src/app/portal/interview/[id]/interview-client.tsx"
      provides: "Portal interview state machine"
    - path: "apps/candidate/src/app/(public)/interview/[token]/interview-client.tsx"
      provides: "Candidate interview state machine"
  key_links:
    - from: "apps/portal"
      to: "packages/shared-video"
      via: "workspace dependency import"
      pattern: "@splits-network/shared-video"
    - from: "apps/candidate"
      to: "packages/shared-video"
      via: "workspace dependency import"
      pattern: "@splits-network/shared-video"
---

<objective>
Final integration pass: fix any build errors across all packages, verify anti-patterns are avoided, add env var configuration, and visually verify the interview flow works.

Purpose: Ensure everything compiles and integrates correctly before marking the phase complete.
Output: Clean builds across all packages + visual verification.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-video-call-experience/34-CONTEXT.md
@.planning/phases/34-video-call-experience/34-04-SUMMARY.md
@.planning/phases/34-video-call-experience/34-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and env var setup</name>
  <files>
    apps/portal/.env.example
    apps/candidate/.env.example
  </files>
  <action>
    Run full build chain and fix any errors:

    1. `pnpm --filter @splits-network/shared-video build` (or tsc --noEmit)
    2. `pnpm --filter @splits-network/portal build`
    3. `pnpm --filter @splits-network/candidate build`
    4. `pnpm --filter @splits-network/video-service build`

    Fix any TypeScript errors, missing imports, or dependency issues.

    Add `NEXT_PUBLIC_LIVEKIT_URL` to .env.example files for both portal and candidate apps:
    ```
    # LiveKit WebSocket URL (public, used by browser to connect to LiveKit)
    NEXT_PUBLIC_LIVEKIT_URL=wss://livekit.yourdomain.com
    ```

    Also add `NEXT_PUBLIC_API_BASE_URL` if not already present (needed by useInterviewToken hook).

    Verify anti-patterns are NOT present:
    - No `LiveKitRoom` inside lobby components (grep check)
    - No `AudioVisualizer` usage (deprecated — should use BarVisualizer or custom)
    - No `usePreviewDevice` usage (deprecated — should use usePreviewTracks)
    - `RoomAudioRenderer` present inside VideoRoom
    - `StartAudio` present inside VideoRoom
  </action>
  <verify>
    All four build commands pass without errors.
    `NEXT_PUBLIC_LIVEKIT_URL` present in both .env.example files.
    No deprecated LiveKit APIs in use (grep confirms).
  </verify>
  <done>All packages build cleanly. Env vars documented. No deprecated LiveKit APIs or anti-patterns present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete video interview experience: shared-video package with lobby and room components, portal interview page with state machine, candidate prep page with magic link flow, and Join Interview button on application detail page.</what-built>
  <how-to-verify>
    1. Start the portal app: `pnpm --filter @splits-network/portal dev`
    2. Start the candidate app: `pnpm --filter @splits-network/candidate dev`
    3. Start the video-service: `pnpm --filter @splits-network/video-service dev`
    4. Start the api-gateway: `pnpm --filter @splits-network/api-gateway dev`

    **Portal flow:**
    5. Navigate to an application detail page that has a scheduled interview
    6. Verify "Join Interview" button is visible
    7. Click it — verify new tab opens at `/portal/interview/{id}`
    8. Verify lobby shows: camera preview (left), interview info (right), device selectors, mic level meter
    9. Click "Join Interview" in lobby — verify it connects to LiveKit and shows in-call view

    **Candidate flow:**
    10. Create an interview with magic link token via API (or use existing test data)
    11. Open candidate app at `/interview/{magic-token}`
    12. Verify prep page shows: job title, company, interviewer, tips, countdown
    13. Click "Enter Lobby" — verify lobby appears with camera preview
    14. Join call — verify video/audio works

    **Note:** Full end-to-end testing requires LiveKit server running. If LiveKit is not available locally, verify that:
    - Pages render without errors
    - Lobby shows camera preview (uses browser getUserMedia, no server needed)
    - State transitions work (lobby buttons trigger state changes)
    - Error states render correctly when LiveKit connection fails
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All apps and packages build without errors
- Portal interview page renders lobby and transitions through states
- Candidate prep page shows interview details and transitions to lobby
- Join Interview button on application detail opens new tab
- No deprecated LiveKit APIs used
- Env vars documented in .env.example files
</verification>

<success_criteria>
- Clean builds across shared-video, portal, candidate, video-service
- Visual verification of lobby and in-call interfaces
- Both join flows work (authenticated + magic link)
- Phase 34 success criteria met per ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/34-video-call-experience/34-06-SUMMARY.md`
</output>
