---
phase: 19-portal-gateway-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-gateway/src/routes/v2/chat.ts
  - services/api-gateway/src/routes/v2/site-notifications.ts
  - services/api-gateway/src/routes/v2/content.ts
  - services/api-gateway/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Api-gateway has no /api/v2/admin/chat routes"
    - "Api-gateway has no admin write routes for site-notifications (POST, PATCH, DELETE, bulk-delete)"
    - "Api-gateway has no admin write routes for content (POST, PATCH, DELETE for pages/images/navigation)"
    - "Api-gateway public read routes for site-notifications, content pages, and navigation still work"
    - "Api-gateway builds cleanly with no errors"
  artifacts:
    - path: "services/api-gateway/src/routes/v2/chat.ts"
      provides: "Chat routes with only user-facing /api/v2/chat routes"
    - path: "services/api-gateway/src/routes/v2/site-notifications.ts"
      provides: "Site notifications with only public GET route"
    - path: "services/api-gateway/src/routes/v2/content.ts"
      provides: "Content routes with only public read routes"
  key_links:
    - from: "services/api-gateway/src/routes/v2/routes.ts"
      to: "services/api-gateway/src/routes/v2/chat.ts"
      via: "registerChatRoutes import"
      pattern: "registerChatRoutes"
---

<objective>
Remove admin-specific routes from the api-gateway so it serves only user-facing API traffic.

Purpose: Api-gateway becomes a clean user-facing gateway. Admin API traffic goes exclusively through admin-gateway.
Output: Api-gateway with no admin routes, clean swagger tags, and passing build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-portal-gateway-cleanup/19-CONTEXT.md
@services/api-gateway/src/routes/v2/chat.ts
@services/api-gateway/src/routes/v2/site-notifications.ts
@services/api-gateway/src/routes/v2/content.ts
@services/api-gateway/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove admin routes from api-gateway</name>
  <files>
    services/api-gateway/src/routes/v2/chat.ts
    services/api-gateway/src/routes/v2/site-notifications.ts
    services/api-gateway/src/routes/v2/content.ts
    services/api-gateway/src/index.ts
  </files>
  <action>
1. In `services/api-gateway/src/routes/v2/chat.ts`:
   - Remove the two `app.all('/api/v2/admin/chat/*', ...)` and `app.all('/api/v2/admin/chat', ...)` route handlers (lines 10-90). These are admin-only routes that now go through admin-gateway.
   - Keep the two user-facing routes: `app.all('/api/v2/chat/*', ...)` and `app.all('/api/v2/chat', ...)` (lines 92-167).

2. In `services/api-gateway/src/routes/v2/site-notifications.ts`:
   - Remove ALL admin write routes: GET /all (admin list), POST (create), PATCH /:id (update), POST /bulk-delete (bulk delete), DELETE /:id (delete). These are tagged as `["admin"]` in their swagger schema.
   - Keep ONLY the public GET `/api/v2/site-notifications` route (lines 9-48) which serves active notifications to all users.
   - Clean up any unused imports after route removal.

3. In `services/api-gateway/src/routes/v2/content.ts`:
   - Remove ALL admin write routes:
     - POST /api/v2/pages (create page)
     - POST /api/v2/pages/import (import page)
     - PATCH /api/v2/pages/:id (update page)
     - DELETE /api/v2/pages/:id (delete page)
     - GET /api/v2/content-images (list images — admin only)
     - GET /api/v2/content-images/:id (get image — admin only)
     - POST /api/v2/content-images (upload image — admin only)
     - PATCH /api/v2/content-images/:id (update image metadata — admin only)
     - DELETE /api/v2/content-images/:id (delete image — admin only)
     - POST /api/v2/navigation (upsert navigation — admin only)
   - Keep ONLY the public read routes:
     - GET /api/v2/pages (list pages)
     - GET /api/v2/pages/by-slug/:slug (get by slug)
     - GET /api/v2/pages/:id (get by ID)
     - GET /api/v2/navigation (get navigation)
     - GET /api/v2/navigation/:id (get navigation by ID)
   - Clean up unused imports (buildAuthHeaders may no longer be needed if all remaining routes are public).

4. In `services/api-gateway/src/index.ts`:
   - Remove the `admin` tag from the swagger tags array (line 192: `{ name: 'admin', description: 'Platform admin and automation' }`)
   - Remove the admin chat rate limit exclusion (line 231: `if (url.startsWith('/api/v2/admin/chat')) return true;`)
   - Also remove the comment on line 390 about site-notifications/all being admin (if it exists after the route removal)

5. Run `pnpm --filter @splits-network/api-gateway build` to verify no broken imports.
  </action>
  <verify>
    pnpm --filter @splits-network/api-gateway build — must succeed with no errors
    grep -r "/api/v2/admin" services/api-gateway/src/ — should return no matches
    grep "tags.*admin" services/api-gateway/src/routes/v2/site-notifications.ts — should return no matches
  </verify>
  <done>
    Api-gateway has no admin-specific routes. Chat, site-notifications, and content files retain only public/user-facing routes. Swagger has no admin tag. Build passes clean.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @splits-network/api-gateway build` passes with zero errors
2. `grep -r "/api/v2/admin" services/api-gateway/src/` returns nothing
3. `grep "tags.*admin" services/api-gateway/src/routes/v2/` returns nothing
4. Chat routes still handle /api/v2/chat (user-facing)
5. Site-notifications still serves public GET /api/v2/site-notifications
6. Content routes still serve public GET for pages and navigation
</verification>

<success_criteria>
Api-gateway serves only user-facing routes. Admin chat, admin site-notification CRUD, admin content CRUD, and admin content-image routes are removed. Public read routes for all three areas remain functional. Build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/19-portal-gateway-cleanup/19-02-SUMMARY.md`
</output>
