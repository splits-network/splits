---
phase: 49-critical-flow-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/call-service/src/v2/service.ts
  - services/call-service/src/v2/call-lifecycle-service.ts
  - services/call-service/src/v2/token-service.ts
  - services/video-service/src/v2/calls/call-recording-webhook.ts
autonomous: true

must_haves:
  truths:
    - "call.created event payload includes participants array with user_id and role"
    - "call.cancelled event payload includes participants array"
    - "call.rescheduled event payload includes participants array"
    - "call.recording_ready event payload includes participants array"
    - "Token generation endpoint returns access_token field matching frontend type"
  artifacts:
    - path: "services/call-service/src/v2/service.ts"
      provides: "call.created event with participants"
      contains: "participants:"
    - path: "services/call-service/src/v2/call-lifecycle-service.ts"
      provides: "cancel/reschedule events with participants"
      contains: "participants:"
    - path: "services/call-service/src/v2/token-service.ts"
      provides: "Token response with access_token field"
      contains: "access_token"
    - path: "services/video-service/src/v2/calls/call-recording-webhook.ts"
      provides: "call.recording_ready event with participants"
      contains: "participants"
  key_links:
    - from: "services/call-service/src/v2/service.ts"
      to: "notification-service consumer"
      via: "RabbitMQ call.created event"
      pattern: "participants.*user_id.*role"
    - from: "services/call-service/src/v2/token-service.ts"
      to: "apps/portal/src/hooks/use-create-call.ts"
      via: "API response field name"
      pattern: "access_token"
---

<objective>
Fix backend event payloads and token response field name so notification emails are delivered and instant call redirects work.

Purpose: 4 RabbitMQ events publish without a `participants` array, causing notification-service to send zero emails. The token endpoint returns `token` but the frontend reads `access_token`, breaking instant call join.
Output: All 4 event publishers include participants; token endpoint returns `access_token`.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v10-MILESTONE-AUDIT.md

@services/call-service/src/v2/service.ts
@services/call-service/src/v2/call-lifecycle-service.ts
@services/call-service/src/v2/token-service.ts
@services/call-service/src/v2/scheduler.ts
@services/call-service/src/v2/participant-repository.ts
@services/video-service/src/v2/calls/call-recording-webhook.ts
@services/video-service/src/v2/calls/call-repository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add participants array to all 4 event payloads</name>
  <files>
    services/call-service/src/v2/service.ts
    services/call-service/src/v2/call-lifecycle-service.ts
    services/video-service/src/v2/calls/call-recording-webhook.ts
  </files>
  <action>
Fix 4 event publishers to include a `participants` array. The notification-service normalizer accepts `{ user_id, role }` (snake_case) via its normalizePayload function.

**call.created** (`services/call-service/src/v2/service.ts:84-95`):
- `enrichedParticipants` is already fetched on line 78 via `this.repository.participants.getCallParticipants(call.id)`.
- Replace `participant_count: createdParticipants.length` with `participants: enrichedParticipants.map(p => ({ user_id: p.user_id, role: p.role }))`.

**call.cancelled** (`services/call-service/src/v2/call-lifecycle-service.ts:99-105`):
- Before the publish call, fetch participants: `const participants = await this.repository.participants.getCallParticipants(id);`
- Add to payload: `participants: participants.map(p => ({ user_id: p.user_id, role: p.role }))`.
- The `repository` property on CallLifecycleService is type `CallRepository` which already has `.participants.getCallParticipants()`.

**call.rescheduled** (`services/call-service/src/v2/call-lifecycle-service.ts:152-158`):
- Before the publish call (~line 148), fetch participants: `const participants = await this.repository.participants.getCallParticipants(id);`
- Add to payload: `participants: participants.map(p => ({ user_id: p.user_id, role: p.role }))`.

**call.recording_ready** (`services/video-service/src/v2/calls/call-recording-webhook.ts:51-56`):
- The webhook handler has access to `callRecordingService` which returns `{ callId }` from `handleEgressComplete`.
- After line 49 (where `callId` is available), fetch participants from the video-service's own call repository. The `callRecordingService` is constructed with a `CallRepository` instance. Add a method or use the repository directly.
- Check: `services/video-service/src/v2/calls/call-recording-service.ts` — it likely has a `repository` property. If so, expose `callId` and fetch participants via `config.callRecordingService.repository.getCallParticipants(callId)`.
- If the repository is not directly accessible from the webhook, pass the repository into the webhook config (it's already available where the webhook is registered) and call `repository.getCallParticipants(callId)`.
- Add to payload: `participants: participants.map(p => ({ user_id: p.user_id, role: p.role }))`.

Do NOT change the notification-service consumer — it already handles both snake_case and camelCase via normalizePayload.
  </action>
  <verify>
Run `pnpm --filter @splits-network/call-service build` and `pnpm --filter @splits-network/video-service build` — both must compile without errors. Grep all 4 event publish calls to confirm each includes a `participants:` field.
  </verify>
  <done>All 4 events (call.created, call.cancelled, call.rescheduled, call.recording_ready) include a participants array with user_id and role for each participant. notification-service resolveParticipantContacts will receive the array and send emails.</done>
</task>

<task type="auto">
  <name>Task 2: Fix token response field name from `token` to `access_token`</name>
  <files>
    services/call-service/src/v2/token-service.ts
  </files>
  <action>
In `services/call-service/src/v2/token-service.ts` line 67, the `generateToken` method returns `{ token, livekit_token: livekitToken }`.

Change the return to: `{ access_token: token, livekit_token: livekitToken }`.

This aligns with the frontend `CallTokenResult` type in `apps/portal/src/hooks/use-create-call.ts:29-33` which expects `{ access_token: string; livekit_token: string; call_id: string }`.

Note: If `call_id` is also expected by the frontend type but not currently returned, add it: `{ access_token: token, livekit_token: livekitToken, call_id: callId }`. Check the method signature — `callId` is already a parameter.

Also check if any other code reads the `token` field from this response (grep for `\.token` in call-service routes that call generateToken). If found, update those references to `access_token`.
  </action>
  <verify>
Run `pnpm --filter @splits-network/call-service build` — must compile. Grep `token-service.ts` for the return statement to confirm it returns `access_token`.
  </verify>
  <done>Token endpoint returns `{ access_token, livekit_token }` matching the frontend CallTokenResult type. The instant call flow in call-creation-modal.tsx will read `tokenResult.access_token` successfully and redirect to the video app with a valid token.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @splits-network/call-service build` passes
- `pnpm --filter @splits-network/video-service build` passes
- All 4 event publish calls include `participants:` array (grep confirms)
- Token service returns `access_token` field (grep confirms)
</verification>

<success_criteria>
1. All 4 RabbitMQ event payloads include `participants` array — notification-service can resolve contacts and send emails
2. Token endpoint returns `access_token` matching frontend type — instant call redirect uses valid token URL
</success_criteria>

<output>
After completion, create `.planning/phases/49-critical-flow-fixes/49-01-SUMMARY.md`
</output>
