---
phase: 41-schedule-and-recording-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/video-service/src/v2/interviews/service.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can schedule an interview from the application detail page and POST succeeds with participants auto-resolved"
    - "Interview record has both an interviewer and candidate participant after creation"
  artifacts:
    - path: "services/video-service/src/v2/interviews/service.ts"
      provides: "Auto-resolution of participants from application when array is empty"
      contains: "candidates"
  key_links:
    - from: "services/video-service/src/v2/interviews/service.ts"
      to: "applications table -> candidates table"
      via: "Supabase query joining application to candidate user_id"
      pattern: "from\\('applications'\\).*candidate"
---

<objective>
Fix BUG-03: Schedule modal sends empty participants array, causing video-service to reject the request with 400.

Purpose: Unblocks the primary interview scheduling flow. Without this fix, no interviews can be created through the UI.

Output: Video-service auto-resolves participants from the application when the participants array is empty — the creator becomes the interviewer, and the application's candidate becomes the candidate participant.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@services/video-service/src/v2/interviews/service.ts
@services/video-service/src/v2/interviews/repository.ts
@services/video-service/src/v2/interviews/types.ts
@apps/portal/src/components/basel/scheduling/schedule-interview-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-resolve participants from application in video-service</name>
  <files>services/video-service/src/v2/interviews/service.ts</files>
  <action>
In `createInterview()` method of `InterviewService`, replace the participant validation block (lines 44-50) with auto-resolution logic:

1. Change the existing application query (line 21-26) to also select the candidate's user_id. The `applications` table has a `candidate_id` FK to the `candidates` table, and `candidates` has a `user_id` column. Update the select to:
   ```
   .select('id, candidate:candidates(user_id)')
   ```

2. Replace the participant validation (lines 44-50) with:
   - If `input.participants` is empty or missing, auto-resolve:
     a. Resolve `createdByUserId` (clerk ID) to internal UUID using `this.repository.resolveUserId(createdByUserId)`
     b. Get the candidate's user_id from the application query result: `application.candidate.user_id`
     c. Build participants array: `[{ user_id: resolvedCreatorId, role: 'interviewer' }, { user_id: candidateUserId, role: 'candidate' }]`
     d. Set `input.participants` to this resolved array
   - If `input.participants` is NOT empty, keep existing behavior (pass through as-is)

3. After auto-resolution, validate that at least one participant exists (defensive check). If the candidate has no user_id (edge case), throw a 400 error: "Could not resolve candidate for this application".

Do NOT modify the frontend modal — the service-side fix is more robust and handles any caller that omits participants.
  </action>
  <verify>
Run `pnpm --filter @splits-network/video-service build` to confirm TypeScript compiles without errors. Verify the service.ts file is under 200 lines. Read the modified file to confirm the auto-resolution logic is correct.
  </verify>
  <done>
When participants array is empty in POST /api/v2/interviews, service auto-resolves creator as interviewer + application candidate as candidate participant. When participants array is provided, existing behavior is unchanged.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @splits-network/video-service build` passes
- The `createInterview` method handles both empty and non-empty participants arrays
- Application query now selects candidate user_id via join
</verification>

<success_criteria>
POST /api/v2/interviews with `participants: []` no longer returns 400. Instead, it auto-resolves the interviewer (from clerk auth) and candidate (from application) as participants.
</success_criteria>

<output>
After completion, create `.planning/phases/41-schedule-and-recording-bug-fixes/41-01-SUMMARY.md`
</output>
