---
phase: 41-schedule-and-recording-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-video/src/hooks/use-recording-state.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Recording status polling reads the correct field from the API response"
    - "Observers and page refreshes show accurate recording state"
  artifacts:
    - path: "packages/shared-video/src/hooks/use-recording-state.ts"
      provides: "Correct field access for recording status polling"
      contains: "recording_status"
  key_links:
    - from: "packages/shared-video/src/hooks/use-recording-state.ts"
      to: "GET /api/v2/interviews/:id/recording"
      via: "apiCall polling every 5 seconds"
      pattern: "data\\.data\\.recording_status"
---

<objective>
Fix BUG-04: Recording status polling hook reads wrong field name from API response.

Purpose: Ensures all participants (not just the interviewer with local state) see accurate recording status, and that recording state survives page refresh.

Output: Hook reads `data.data.recording_status` instead of `data.data.status`, matching the actual API response shape.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/shared-video/src/hooks/use-recording-state.ts
@services/video-service/src/v2/interviews/recording-routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix recording status field name in polling hook</name>
  <files>packages/shared-video/src/hooks/use-recording-state.ts</files>
  <action>
In `use-recording-state.ts`, around line 93, change the polling callback field access:

Before: `if (data?.data?.status) { setRecordingStatus(data.data.status); }`
After: `if (data?.data?.recording_status) { setRecordingStatus(data.data.recording_status); }`

The GET /api/v2/interviews/:id/recording endpoint returns `{ data: { recording_status: '...' } }` (see recording-routes.ts line 98), but the hook reads `data.data.status` which is always undefined.

This is a single-line field name change. Do NOT change any other logic in the hook.
  </action>
  <verify>
Run `pnpm --filter @splits-network/shared-video build` to confirm TypeScript compiles. Read the modified file to confirm only the field name was changed and no other logic was altered.
  </verify>
  <done>
Polling hook reads `recording_status` from API response. Observers see correct recording state. Recording status survives page refresh.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @splits-network/shared-video build` passes
- Field access matches API response shape: `data.data.recording_status`
- No other changes to hook logic
</verification>

<success_criteria>
Recording status polling correctly reads `recording_status` from the API response, enabling accurate state display for all participants and across page refreshes.
</success_criteria>

<output>
After completion, create `.planning/phases/41-schedule-and-recording-bug-fixes/41-02-SUMMARY.md`
</output>
