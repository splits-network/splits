---
phase: 51-recording-consent-calendar-auto-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/call-service/src/v2/repository.ts
  - services/call-service/src/v2/types.ts
  - apps/video/src/lib/types.ts
  - apps/video/src/lib/call-adapter.ts
  - apps/video/src/components/call-experience.tsx
autonomous: true

must_haves:
  truths:
    - "Recording consent banner appears in the video lobby when a call has recording enabled"
    - "The banner is NOT shown for internal_call type calls (requires_recording_consent: false)"
  artifacts:
    - path: "apps/video/src/lib/call-adapter.ts"
      provides: "recording_enabled derived from call_type metadata"
      contains: "recording_enabled"
    - path: "apps/video/src/components/call-experience.tsx"
      provides: "recordingEnabled prop passed to VideoLobby"
      contains: "recordingEnabled"
  key_links:
    - from: "services/call-service/src/v2/repository.ts"
      to: "call_types table"
      via: "join on call_type to get requires_recording_consent"
      pattern: "call_types"
    - from: "apps/video/src/lib/call-adapter.ts"
      to: "CallDetail.recording_consent_required"
      via: "maps to CallContext.recording_enabled"
      pattern: "recording_enabled.*recording_consent_required"
---

<objective>
Pass recording consent data from call_types lookup through the entire chain so the video app's VideoLobby displays the recording consent banner.

Purpose: The RecordingConsent component and VideoLobby consent gate already work correctly, but call-experience.tsx never passes `recordingEnabled` because the adapter hardcodes it to `false`. The `call_types` table has `requires_recording_consent` per type but this data never reaches the frontend.

Output: Recording consent banner displays in video lobby for interview and client_meeting call types.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@services/call-service/src/v2/types.ts
@services/call-service/src/v2/repository.ts
@apps/video/src/lib/types.ts
@apps/video/src/lib/call-adapter.ts
@apps/video/src/components/call-experience.tsx
@packages/shared-video/src/components/video-lobby.tsx
@packages/shared-video/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recording_consent_required to call detail response</name>
  <files>
    services/call-service/src/v2/types.ts
    services/call-service/src/v2/repository.ts
  </files>
  <action>
**types.ts** — Add `recording_consent_required` to the `CallDetail` interface:
- Add `recording_consent_required?: boolean` to the `CallDetail` interface (optional since existing endpoints may not include it)

**repository.ts** — In `getCallDetail()`, after fetching the call, look up the `call_types` row to get `requires_recording_consent`:
- After `const call = await this.getCall(id);` and before building the detail object
- Query: `this.supabase.from('call_types').select('requires_recording_consent').eq('slug', call.call_type).maybeSingle()`
- Set `detail.recording_consent_required = callType?.requires_recording_consent ?? true` (default true for safety)
- This is a single extra query but it's a tiny table lookup by primary key

Do NOT modify the `getCall()` method (used for simple operations that don't need this data).
Do NOT add a join — the call_types table is a lookup table queried separately.
  </action>
  <verify>
    `pnpm --filter @splits-network/call-service build` succeeds with no type errors
  </verify>
  <done>
    CallDetail includes recording_consent_required field populated from call_types lookup table
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire recording consent through video app</name>
  <files>
    apps/video/src/lib/types.ts
    apps/video/src/lib/call-adapter.ts
    apps/video/src/components/call-experience.tsx
  </files>
  <action>
**apps/video/src/lib/types.ts** — Add `recording_consent_required` to `CallDetail` interface:
- Add `recording_consent_required?: boolean` field

**apps/video/src/lib/call-adapter.ts** — Use the new field instead of hardcoded `false`:
- Change line 19 from `recording_enabled: false,` to `recording_enabled: call.recording_consent_required ?? true,`
- Default to `true` for safety (show consent if data is missing)

**apps/video/src/components/call-experience.tsx** — Pass `recordingEnabled` to VideoLobby:
- The `callContext` already contains `recording_enabled` from the adapter
- In both VideoLobby render locations (lobby and connecting states), add the prop: `recordingEnabled={callContext.recording_enabled}`
- The VideoLobby already accepts this prop and handles consent gating + banner display
- Do NOT add `onRecordingConsent` — recording consent API submission is separate future work; the visual banner is sufficient for this phase

There are two VideoLobby renders in CallStateRouter (one in 'prep'/'lobby' case, one in 'connecting' case). Add `recordingEnabled` to BOTH.
  </action>
  <verify>
    `pnpm --filter @splits-network/video build` succeeds with no type errors
  </verify>
  <done>
    VideoLobby receives recordingEnabled prop derived from call_types.requires_recording_consent; consent banner displays for interview and client_meeting call types
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @splits-network/call-service build` — no errors
2. `pnpm --filter @splits-network/video build` — no errors
3. In call-adapter.ts: `recording_enabled` is NOT hardcoded to `false`
4. In call-experience.tsx: `recordingEnabled` prop is passed to VideoLobby
5. In repository.ts: `call_types` is queried for `requires_recording_consent`
</verification>

<success_criteria>
- call-service CallDetail response includes recording_consent_required from call_types lookup
- Video app adapter maps recording_consent_required to recording_enabled in CallContext
- VideoLobby receives recordingEnabled prop in both lobby and connecting states
- Recording consent banner will display for call types where requires_recording_consent is true (interview, client_meeting)
</success_criteria>

<output>
After completion, create `.planning/phases/51-recording-consent-calendar-auto-creation/51-01-SUMMARY.md`
</output>
