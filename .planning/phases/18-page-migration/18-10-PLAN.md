---
phase: 18-page-migration
plan: 10
type: execute
wave: 4
depends_on: ["18-05", "18-06", "18-07", "18-08", "18-09"]
files_modified:
  - apps/portal/src/app/portal/admin/
autonomous: false

must_haves:
  truths:
    - "Portal admin pages are removed after admin app pages are verified working"
    - "Portal admin layout and shared components are removed"
    - "Portal admin hooks and admin-specific code are removed"
    - "No broken imports remain in portal after removal"
  artifacts:
    - path: "apps/portal/src/app/portal/admin/"
      provides: "Directory should be mostly empty or contain only redirect stub"
  key_links:
    - from: "apps/portal/src/app/portal/admin/page.tsx"
      to: "admin app URL"
      via: "redirect or link to admin app"
---

<objective>
Remove portal admin pages now that all admin functionality lives in the dedicated admin app.

Purpose: Per the migration strategy, portal admin pages are removed as admin app pages are completed. This reduces portal complexity and prevents stale code.
Output: Portal admin directory cleared of migrated pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-page-migration/18-CONTEXT.md
@.planning/phases/18-page-migration/18-06-SUMMARY.md
@.planning/phases/18-page-migration/18-07-SUMMARY.md
@.planning/phases/18-page-migration/18-08-SUMMARY.md
@.planning/phases/18-page-migration/18-09-SUMMARY.md
@apps/portal/src/app/portal/admin/layout.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All admin pages have been migrated to the admin app (plans 05-09). Before removing portal admin pages, verify the admin app is working.</what-built>
  <how-to-verify>
    1. Start the admin app: `pnpm --filter @splits-network/admin dev`
    2. Navigate to the admin app URL (http://localhost:3200)
    3. Sign in and verify you reach the dashboard at /secure/
    4. Verify sidebar shows all sections with navigation working
    5. Spot-check a few pages: /secure/recruiters, /secure/jobs, /secure/settings
    6. Verify data tables load (or show appropriate empty/error states)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with portal admin page removal, or describe issues to fix first</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remove portal admin pages and components</name>
  <files>
    apps/portal/src/app/portal/admin/
  </files>
  <action>
    Remove all portal admin pages, components, and hooks that have been migrated to the admin app:

    1. **Delete all page directories** under `apps/portal/src/app/portal/admin/`:
       - activity/, applications/, assignments/, automation/, billing-profiles/, candidates/, chat/, companies/, content/, decision-log/, fraud/, jobs/, matches/, metrics/, notifications/, organizations/, ownership/, payouts/, placements/, recruiters/, reputation/, settings/, users/

    2. **Delete admin components**: `apps/portal/src/app/portal/admin/components/` directory entirely

    3. **Delete admin hooks**: `apps/portal/src/app/portal/admin/hooks/` directory entirely

    4. **Delete admin dashboard client**: `apps/portal/src/app/portal/admin/admin-dashboard-client.tsx`

    5. **Keep admin layout.tsx** but replace its content with a redirect to the admin app:
       ```tsx
       import { redirect } from 'next/navigation';
       export default function AdminLayout() {
           redirect(process.env.NEXT_PUBLIC_ADMIN_APP_URL || 'http://localhost:3200');
       }
       ```

    6. **Keep admin page.tsx** but replace with redirect:
       ```tsx
       import { redirect } from 'next/navigation';
       export default function AdminPage() {
           redirect(process.env.NEXT_PUBLIC_ADMIN_APP_URL || 'http://localhost:3200');
       }
       ```

    7. **Verify no broken imports**: Run `pnpm --filter @splits-network/portal build` to check for any import errors. Fix any remaining references to deleted admin components/hooks.

    8. **Check portal sidebar**: If the portal sidebar has admin navigation links, update them to point to the admin app URL or remove them. The portal sidebar should either show an external link "Admin App" or not show admin nav at all.
  </action>
  <verify>
    `pnpm --filter @splits-network/portal build` compiles without errors.
    `ls apps/portal/src/app/portal/admin/` shows only layout.tsx and page.tsx (redirect stubs).
    No orphaned imports to deleted admin files.
  </verify>
  <done>Portal admin pages removed. Admin layout/page redirect to admin app. Portal builds clean.</done>
</task>

</tasks>

<verification>
- Portal builds without import errors
- Portal admin routes redirect to admin app
- Portal admin directory contains only redirect stubs
- No admin-specific hooks or components remain in portal
</verification>

<success_criteria>
Portal admin pages are removed with clean redirects to the admin app. Portal compiles successfully with no broken imports.
</success_criteria>

<output>
After completion, create `.planning/phases/18-page-migration/18-10-SUMMARY.md`
</output>
