---
phase: 18-page-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - services/identity-service/src/v2/admin/routes.ts
  - services/identity-service/src/v2/admin/repository.ts
  - services/identity-service/src/v2/admin/service.ts
  - services/ats-service/src/v2/admin/routes.ts
  - services/ats-service/src/v2/admin/repository.ts
  - services/ats-service/src/v2/admin/service.ts
  - services/network-service/src/v2/admin/routes.ts
  - services/network-service/src/v2/admin/repository.ts
  - services/network-service/src/v2/admin/service.ts
  - services/billing-service/src/v2/admin/routes.ts
  - services/billing-service/src/v2/admin/repository.ts
  - services/notification-service/src/v2/admin/routes.ts
  - services/notification-service/src/v2/admin/repository.ts
  - services/admin-gateway/src/routes.ts
autonomous: true

must_haves:
  truths:
    - "Admin-gateway routes /admin/{service}/admin/* to domain services /admin/* endpoints"
    - "Domain services expose /admin/ prefixed routes with permissive queries"
    - "Admin routes return full entity data without restrictive access filtering"
    - "Admin count endpoint returns aggregate counts for sidebar badges"
  artifacts:
    - path: "services/identity-service/src/v2/admin/routes.ts"
      provides: "Admin routes for users, recruiters, organizations"
    - path: "services/ats-service/src/v2/admin/routes.ts"
      provides: "Admin routes for jobs, applications, candidates, assignments, placements"
    - path: "services/network-service/src/v2/admin/routes.ts"
      provides: "Admin routes for companies, matches"
    - path: "services/billing-service/src/v2/admin/routes.ts"
      provides: "Admin routes for payouts, escrow, billing profiles"
    - path: "services/notification-service/src/v2/admin/routes.ts"
      provides: "Admin routes for notifications"
  key_links:
    - from: "services/admin-gateway/src/routes.ts"
      to: "services/*/src/v2/admin/routes.ts"
      via: "HTTP proxy /admin/{service}/admin/* -> /admin/*"
    - from: "services/identity-service/src/v2/admin/routes.ts"
      to: "services/identity-service/src/v2/admin/repository.ts"
      via: "repository method calls"
---

<objective>
Create /admin/ prefixed routes in domain services for the admin app, with permissive queries and an aggregate counts endpoint.

Purpose: Admin pages need dedicated backend endpoints that return full data without user-scoped access restrictions. The /admin/ prefix cleanly separates admin API surface from user-facing endpoints.
Output: Admin route files in 5 domain services, plus admin-gateway routing updates.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-page-migration/18-CONTEXT.md
@services/admin-gateway/src/routes.ts
@services/identity-service/src/v2/users/routes.ts
@services/ats-service/src/v2/jobs/routes.ts
@services/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin routes in identity-service and ats-service</name>
  <files>
    services/identity-service/src/v2/admin/routes.ts
    services/identity-service/src/v2/admin/repository.ts
    services/identity-service/src/v2/admin/service.ts
    services/identity-service/src/index.ts
    services/ats-service/src/v2/admin/routes.ts
    services/ats-service/src/v2/admin/repository.ts
    services/ats-service/src/v2/admin/service.ts
    services/ats-service/src/index.ts
  </files>
  <action>
    Follow existing V2 service patterns for route/repository/service files. Look at existing v2 route files for patterns.

    **Identity Service** (`services/identity-service/src/v2/admin/`):

    1. **repository.ts** — `AdminIdentityRepository`:
       - `listUsersAdmin(params)`: All users, paginated, searchable, sortable. No access filtering. Include role data.
       - `getUserAdmin(id)`: Single user with full profile + roles + organizations
       - `listRecruitersAdmin(params)`: All recruiters with joined user data, filterable by status
       - `updateRecruiterStatusAdmin(id, status)`: Update recruiter status
       - `listOrganizationsAdmin(params)`: All organizations, paginated, searchable
       - `getAdminCounts()`: Returns `{ users: count, recruiters_pending: count, organizations: count }` in one query
       - Use existing supabase client patterns from other repositories

    2. **service.ts** — `AdminIdentityService`: Thin layer calling repository methods

    3. **routes.ts** — Register under `/admin` prefix:
       - `GET /admin/users` — list with pagination/search/sort
       - `GET /admin/users/:id` — single user detail
       - `GET /admin/recruiters` — list with status filter
       - `PATCH /admin/recruiters/:id/status` — update status
       - `GET /admin/organizations` — list with pagination/search
       - `GET /admin/counts` — aggregate counts for sidebar
       - Register routes in the service's index.ts

    **ATS Service** (`services/ats-service/src/v2/admin/`):

    1. **repository.ts** — `AdminAtsRepository`:
       - `listJobsAdmin(params)`: All jobs, paginated, searchable, sortable, filterable by status/commute_type/job_level
       - `getJobAdmin(id)`: Single job with full details
       - `listApplicationsAdmin(params)`: All applications with joined job+candidate data
       - `listCandidatesAdmin(params)`: All candidates, paginated, searchable
       - `listAssignmentsAdmin(params)`: All assignments with joined data
       - `listPlacementsAdmin(params)`: All placements with joined data
       - `getAdminCounts()`: Returns `{ jobs: count, applications: count, candidates: count, assignments: count, placements: count }`

    2. **service.ts** — `AdminAtsService`: Thin layer

    3. **routes.ts** — Register under `/admin` prefix:
       - `GET /admin/jobs`, `GET /admin/jobs/:id`
       - `GET /admin/applications`
       - `GET /admin/candidates`
       - `GET /admin/assignments`
       - `GET /admin/placements`
       - `GET /admin/counts`
       - Register in index.ts

    All list endpoints accept: `page`, `limit`, `search`, `sort_by`, `sort_order`, plus entity-specific filters.
    All responses use `{ data: [...], pagination: { page, limit, total, total_pages } }` envelope.
  </action>
  <verify>
    `pnpm --filter @splits-network/identity-service build` compiles.
    `pnpm --filter @splits-network/ats-service build` compiles.
    Admin routes are registered in each service's index.ts.
  </verify>
  <done>Identity and ATS services have /admin/ routes with permissive queries, pagination, search, and counts endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Create admin routes in network, billing, notification services + gateway routing</name>
  <files>
    services/network-service/src/v2/admin/routes.ts
    services/network-service/src/v2/admin/repository.ts
    services/network-service/src/index.ts
    services/billing-service/src/v2/admin/routes.ts
    services/billing-service/src/v2/admin/repository.ts
    services/billing-service/src/index.ts
    services/notification-service/src/v2/admin/routes.ts
    services/notification-service/src/v2/admin/repository.ts
    services/notification-service/src/index.ts
    services/admin-gateway/src/routes.ts
  </files>
  <action>
    **Network Service** (`services/network-service/src/v2/admin/`):
    - `listCompaniesAdmin(params)`: All companies, paginated, searchable
    - `listMatchesAdmin(params)`: All matches with scores, filterable
    - `getMatchAdmin(id)`: Single match detail with factor breakdown
    - `getAdminCounts()`: `{ companies: count, matches: count }`
    - Routes: `GET /admin/companies`, `GET /admin/matches`, `GET /admin/matches/:id`, `GET /admin/counts`

    **Billing Service** (`services/billing-service/src/v2/admin/`):
    - `listPayoutsAdmin(params)`: All payout transactions, filterable by status
    - `listEscrowHoldsAdmin(params)`: All escrow holds, filterable by status
    - `listBillingProfilesAdmin(params)`: All billing profiles
    - `getAdminCounts()`: `{ payouts_pending: count, escrow_active: count, billing_profiles: count }`
    - Routes: `GET /admin/payouts`, `GET /admin/escrow`, `GET /admin/billing-profiles`, `GET /admin/counts`

    **Notification Service** (`services/notification-service/src/v2/admin/`):
    - `listNotificationsAdmin(params)`: All site notifications, filterable by is_active
    - `createNotificationAdmin(data)`: Create site notification
    - `updateNotificationAdmin(id, data)`: Update notification
    - `deleteNotificationAdmin(id)`: Delete notification
    - `getAdminCounts()`: `{ active_notifications: count }`
    - Routes: `GET /admin/notifications`, `POST /admin/notifications`, `PATCH /admin/notifications/:id`, `DELETE /admin/notifications/:id`, `GET /admin/counts`

    **Admin Gateway** (`services/admin-gateway/src/routes.ts`):
    - The current proxy setup already forwards `/admin/{service}/*` to domain services with prefix stripping to `/`.
    - This means `/admin/identity/admin/users` would be stripped to `/admin/users` on the domain service — which is correct.
    - Verify this works with the /admin/ prefix on domain services. The current `rewritePrefix: ''` strips the `/admin/{service}` prefix, leaving `/admin/users` intact. This is the desired behavior.
    - No changes needed to routes.ts if the proxy is working correctly. Just verify the path math.

    Register all new admin routes in each service's index.ts file.
  </action>
  <verify>
    `pnpm --filter @splits-network/network-service build` compiles.
    `pnpm --filter @splits-network/billing-service build` compiles.
    `pnpm --filter @splits-network/notification-service build` compiles.
    Admin routes are registered in each service.
  </verify>
  <done>Network, billing, and notification services have /admin/ routes. Admin-gateway proxy correctly routes to domain service /admin/* endpoints.</done>
</task>

</tasks>

<verification>
- All 5 domain services compile with new admin routes
- Each service has GET /admin/counts endpoint returning relevant counts
- Admin-gateway proxy routing correctly maps paths
- All list endpoints support pagination, search, sort params
</verification>

<success_criteria>
Admin-gateway can proxy requests to /admin/{service}/admin/* and reach the new admin endpoints in each domain service. Counts endpoint exists for sidebar badges.
</success_criteria>

<output>
After completion, create `.planning/phases/18-page-migration/18-03-SUMMARY.md`
</output>
