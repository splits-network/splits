---
phase: 18-page-migration
plan: 04
type: execute
wave: 2
depends_on: ["18-02", "18-03"]
files_modified:
  - apps/admin/src/app/secure/layout.tsx
  - apps/admin/src/components/sidebar/admin-sidebar.tsx
  - apps/admin/src/components/sidebar/sidebar-section.tsx
  - apps/admin/src/components/sidebar/sidebar-item.tsx
  - apps/admin/src/components/sidebar/sidebar-search.tsx
  - apps/admin/src/components/sidebar/nav-config.ts
  - apps/admin/src/components/header/admin-header.tsx
  - apps/admin/src/components/header/breadcrumbs.tsx
  - apps/admin/src/components/shared/admin-data-table.tsx
  - apps/admin/src/components/shared/admin-page-header.tsx
  - apps/admin/src/components/shared/admin-confirm-modal.tsx
  - apps/admin/src/components/shared/admin-toast.tsx
  - apps/admin/src/components/shared/admin-empty-state.tsx
  - apps/admin/src/components/shared/admin-error-state.tsx
  - apps/admin/src/components/shared/admin-loading-state.tsx
  - apps/admin/src/components/shared/index.ts
  - apps/admin/src/providers/toast-provider.tsx
  - apps/admin/src/hooks/use-admin-toast.ts
autonomous: true

must_haves:
  truths:
    - "Admin layout has header bar with branding, breadcrumbs, and Clerk UserButton"
    - "Sidebar has collapsible sections with icons, search filter, and real-time badge counts"
    - "Sidebar collapses to icon-only rail with tooltips on hover"
    - "Sidebar state persists in localStorage (collapsed/expanded, open sections)"
    - "Mobile responsive with hamburger menu opening sidebar drawer"
    - "Shared data table component renders consistently across all list pages"
    - "Toast notifications work via Basel pattern"
    - "Confirmation modal works via Basel pattern"
  artifacts:
    - path: "apps/admin/src/components/sidebar/admin-sidebar.tsx"
      provides: "Main sidebar component with collapse, search, responsive behavior"
    - path: "apps/admin/src/components/sidebar/nav-config.ts"
      provides: "Consolidated navigation sections (reduced from 9 to 6-7)"
    - path: "apps/admin/src/components/header/admin-header.tsx"
      provides: "Top header bar with branding, breadcrumbs, UserButton"
    - path: "apps/admin/src/components/shared/admin-data-table.tsx"
      provides: "Reusable Basel-styled data table with sorting, selection, actions"
    - path: "apps/admin/src/app/secure/layout.tsx"
      provides: "Secure layout with sidebar, header, providers"
  key_links:
    - from: "apps/admin/src/components/sidebar/admin-sidebar.tsx"
      to: "apps/admin/src/hooks/use-realtime-counts.ts"
      via: "useRealtimeCounts() for live badge counts"
    - from: "apps/admin/src/app/secure/layout.tsx"
      to: "apps/admin/src/providers/realtime-provider.tsx"
      via: "RealtimeProvider wrapping children"
---

<objective>
Build the admin app layout: header, collapsible sidebar with search and real-time badges, and shared page components (data table, toast, confirm modal, page header).

Purpose: All admin pages share the same layout shell. Building this first means all page migration plans can focus purely on page content.
Output: Complete admin layout with sidebar, header, breadcrumbs, and shared components for all pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-page-migration/18-CONTEXT.md
@.planning/phases/18-page-migration/18-02-SUMMARY.md
@apps/admin/src/app/secure/layout.tsx
@apps/portal/src/app/portal/admin/components/admin-sidebar.tsx
@showcase/tables/one/page.tsx
@showcase/dashboards/one/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build admin header, sidebar with collapsible rail, search, and real-time badges</name>
  <files>
    apps/admin/src/components/sidebar/admin-sidebar.tsx
    apps/admin/src/components/sidebar/sidebar-section.tsx
    apps/admin/src/components/sidebar/sidebar-item.tsx
    apps/admin/src/components/sidebar/sidebar-search.tsx
    apps/admin/src/components/sidebar/nav-config.ts
    apps/admin/src/components/header/admin-header.tsx
    apps/admin/src/components/header/breadcrumbs.tsx
    apps/admin/src/app/secure/layout.tsx
  </files>
  <action>
    **nav-config.ts**: Define consolidated sidebar navigation. Reduce from 9 sections to 6:

    ```
    1. Overview: Dashboard
    2. Operations: Recruiters, Assignments, Placements, Applications, Notifications
    3. Directory: Users, Organizations, Companies, Jobs, Candidates
    4. Intelligence: Matches, Automation Rules, Fraud Detection, Decision Log, Chat Moderation, Ownership Audit, Reputation
    5. Finance: Payouts, Escrow Holds, Billing Profiles
    6. Content: Pages, Navigation, Images, Metrics, Activity Log
    7. Settings: Platform Settings
    ```

    Each section: `{ id, title, icon, items: [{href, label, icon, badgeKey?}] }`.
    All hrefs use `/secure/` prefix (e.g., `/secure/recruiters`).
    `badgeKey` maps to useRealtimeCounts keys where applicable.

    **admin-sidebar.tsx**: Main sidebar component:
    - `isCollapsed` state persisted to `localStorage` key `admin-sidebar-collapsed`
    - `openSections` state persisted to `localStorage` key `admin-sidebar-sections`
    - When collapsed: width shrinks to icon-only rail (~w-16), section titles hide, page labels show as tooltips on hover (use DaisyUI tooltip)
    - Toggle button: positioned at top-right edge, straddling the border (absolute, right: -12px, z-10). Uses chevron icon pointing left/right.
    - Smooth animated transitions: `transition-all duration-300` on width
    - Auto-expand section containing active route
    - Dividers between sections in collapsed mode
    - Uses `useRealtimeCounts()` for badge numbers
    - `'use client'` directive

    **sidebar-section.tsx**: Individual collapsible section (extracted for 200-line limit):
    - Expand/collapse with animated max-height transition
    - Badge count sum for section
    - Collapsed mode: show section icon with divider

    **sidebar-item.tsx**: Individual nav item:
    - Active state indicator (left border bar, bg-primary/10)
    - Badge display (warning badge for counts > 0)
    - Tooltip in collapsed mode (DaisyUI `tooltip tooltip-right`)

    **sidebar-search.tsx**: Search/filter input at top of sidebar:
    - Input field that filters nav items by name and keywords
    - When filtering: auto-expand matching sections, hide non-matching items
    - Clear button to reset
    - In collapsed mode: shows only magnifying glass icon, expands sidebar on click

    **admin-header.tsx**: Top header bar:
    - Fixed at top, full width
    - Left: "Splits Admin" branding text (font-black)
    - Center: Breadcrumbs component
    - Right: Clerk UserButton (import UserButtonClient from existing)
    - Height: h-14 or h-16
    - bg-base-200 border-b border-base-300
    - Mobile: hamburger button to open sidebar drawer

    **breadcrumbs.tsx**: Dynamic breadcrumbs from pathname:
    - Parse pathname segments after /secure/
    - Map segments to readable labels (e.g., 'recruiters' -> 'Recruiters')
    - Link all segments except last (current page)
    - DaisyUI breadcrumbs component

    **secure/layout.tsx**: Update to include:
    - Keep existing auth check (userId, isPlatformAdmin gate)
    - Wrap children in RealtimeProvider, QueryProvider, ToastProvider
    - Render AdminHeader at top
    - Render AdminSidebar + main content area in flex row below header
    - Main content: `flex-1 overflow-y-auto p-6`
    - `'use client'` portions may need client component extraction

    Mobile responsive:
    - Desktop (lg+): sidebar visible, fixed beside content
    - Mobile (<lg): sidebar hidden, hamburger in header opens drawer overlay
    - DaisyUI drawer component for mobile
  </action>
  <verify>
    `pnpm --filter @splits-network/admin build` compiles.
    All sidebar files under 200 lines each.
    Layout file integrates header + sidebar + providers.
  </verify>
  <done>Admin layout has collapsible sidebar with search, real-time badges, header with breadcrumbs, and responsive mobile drawer</done>
</task>

<task type="auto">
  <name>Task 2: Build shared page components (data table, page header, toast, confirm modal, states)</name>
  <files>
    apps/admin/src/components/shared/admin-data-table.tsx
    apps/admin/src/components/shared/admin-page-header.tsx
    apps/admin/src/components/shared/admin-confirm-modal.tsx
    apps/admin/src/components/shared/admin-toast.tsx
    apps/admin/src/components/shared/admin-empty-state.tsx
    apps/admin/src/components/shared/admin-error-state.tsx
    apps/admin/src/components/shared/admin-loading-state.tsx
    apps/admin/src/components/shared/index.ts
    apps/admin/src/providers/toast-provider.tsx
    apps/admin/src/hooks/use-admin-toast.ts
  </files>
  <action>
    All components follow Basel design from showcase. `'use client'` where needed.

    **admin-data-table.tsx**: Reusable data table following showcase/tables/one pattern:
    - Generic props: `columns: Column<T>[]`, `data: T[]`, `loading?`, `onSort?`, `sortField?`, `sortDir?`, `selectable?`, `onSelect?`, `selected?`, `actions?: (item: T) => ReactNode`, `onRowClick?`
    - Column definition: `{ key: string, label: string, sortable?: boolean, render?: (item: T) => ReactNode, width?: string }`
    - Basel styling: `table table-sm`, `bg-base-200` header, hover rows, checkbox selection
    - Sort icons in sortable column headers
    - Bulk selection: select-all checkbox in header
    - Row actions: dropdown menu (DaisyUI dropdown) per row
    - Empty state integrated: shows AdminEmptyState when data is empty
    - Loading state: skeleton rows when loading=true
    - Responsive: horizontal scroll on mobile (`overflow-x-auto`)

    **admin-page-header.tsx**: Basel-style page header (NOT the old PageTitle component):
    - Props: `title`, `subtitle?`, `actions?: ReactNode` (buttons in top-right)
    - Clean layout: title left, actions right
    - No breadcrumbs (header handles that)
    - Under 60 lines

    **admin-confirm-modal.tsx**: Basel confirmation modal:
    - Props: `isOpen`, `onClose`, `onConfirm`, `title`, `message`, `confirmLabel?`, `confirmVariant?: 'error' | 'warning' | 'primary'`, `loading?`
    - Uses DaisyUI `dialog` element with `modal` class
    - Not shared-ui ConfirmDialog — standalone Basel pattern

    **admin-toast.tsx** + **toast-provider.tsx** + **use-admin-toast.ts**: Basel toast system:
    - Toast provider with context, renders toasts in fixed bottom-right
    - `useAdminToast()` returns `{ success(msg), error(msg), info(msg), warning(msg) }`
    - Auto-dismiss after 5s, manual close button
    - DaisyUI alert classes for styling
    - Max 5 visible toasts, FIFO removal

    **admin-empty-state.tsx**: Empty state with icon, title, description. Props: `icon`, `title`, `description?`, `action?: ReactNode`.

    **admin-error-state.tsx**: Error state with retry. Props: `message`, `onRetry?`.

    **admin-loading-state.tsx**: Loading skeleton. Can show card skeleton or table skeleton based on `variant?: 'card' | 'table'`.

    **index.ts**: Barrel export all shared components.
  </action>
  <verify>
    `pnpm --filter @splits-network/admin build` compiles.
    All shared component files under 200 lines.
    index.ts exports all components.
  </verify>
  <done>Shared admin components exist: data table, page header, confirm modal, toast system, empty/error/loading states — all Basel design</done>
</task>

</tasks>

<verification>
- Admin app builds successfully
- Navigating to /secure/ shows header + sidebar layout
- All shared components export from index.ts
- Sidebar shows consolidated 6-7 sections
- All files under 200 lines
</verification>

<success_criteria>
Admin /secure/ layout renders header with branding/breadcrumbs/UserButton, collapsible sidebar with search and badge counts, and content area. Shared components are ready for page migration plans to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/18-page-migration/18-04-SUMMARY.md`
</output>
