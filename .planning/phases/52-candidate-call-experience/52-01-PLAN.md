---
phase: 52-candidate-call-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/candidate/src/app/portal/dashboard/hooks/use-upcoming-calls.ts
  - apps/candidate/src/app/portal/dashboard/components/upcoming-calls-widget.tsx
  - apps/candidate/src/app/portal/dashboard/components/candidate-dashboard.tsx
  - apps/candidate/src/hooks/use-call-token.ts
  - apps/candidate/src/app/portal/calls/[id]/join/page.tsx
autonomous: true

must_haves:
  truths:
    - "Candidate dashboard shows upcoming scheduled calls with title, time, participants, and Join Call button"
    - "Clicking Join Call generates a token and redirects to video.applicant.network/join/{token}"
  artifacts:
    - path: "apps/candidate/src/app/portal/dashboard/hooks/use-upcoming-calls.ts"
      provides: "Hook to fetch upcoming calls from GET /calls?status=scheduled"
      exports: ["useUpcomingCalls"]
    - path: "apps/candidate/src/app/portal/dashboard/components/upcoming-calls-widget.tsx"
      provides: "Dashboard widget rendering upcoming call cards"
      exports: ["default"]
    - path: "apps/candidate/src/hooks/use-call-token.ts"
      provides: "Hook to generate call access token via POST /calls/{id}/token"
      exports: ["useCallToken"]
    - path: "apps/candidate/src/app/portal/calls/[id]/join/page.tsx"
      provides: "Join call page that generates token and redirects to video app"
      exports: ["default"]
  key_links:
    - from: "apps/candidate/src/app/portal/dashboard/hooks/use-upcoming-calls.ts"
      to: "/api/v2/calls"
      via: "createAuthenticatedClient GET with status=scheduled filter"
      pattern: "client\\.get.*calls.*status.*scheduled"
    - from: "apps/candidate/src/app/portal/calls/[id]/join/page.tsx"
      to: "/api/v2/calls/{id}/token"
      via: "useCallToken hook POST"
      pattern: "calls.*token"
    - from: "apps/candidate/src/app/portal/calls/[id]/join/page.tsx"
      to: "NEXT_PUBLIC_VIDEO_APP_URL"
      via: "window.location.href redirect"
      pattern: "video.*applicant.*network|NEXT_PUBLIC_VIDEO_APP_URL"
---

<objective>
Add an "Upcoming Calls" widget to the candidate dashboard and create the join-call flow so candidates can see scheduled calls and join them via video.applicant.network.

Purpose: Candidates currently have no visibility into calls scheduled with them. This gives them dashboard awareness and a working join flow.
Output: Dashboard widget with upcoming calls, join call page with token generation and redirect.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/52-candidate-call-experience/52-RESEARCH.md

# Existing patterns to mirror
@apps/candidate/src/app/portal/dashboard/components/match-preview-widget.tsx
@apps/candidate/src/app/portal/dashboard/hooks/use-candidate-dashboard-data.ts
@apps/candidate/src/app/portal/dashboard/components/candidate-dashboard.tsx
@apps/portal/src/app/portal/calls/[id]/join/page.tsx
@apps/portal/src/hooks/use-create-call.ts
@apps/candidate/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create upcoming calls hook and dashboard widget</name>
  <files>
    apps/candidate/src/app/portal/dashboard/hooks/use-upcoming-calls.ts
    apps/candidate/src/app/portal/dashboard/components/upcoming-calls-widget.tsx
    apps/candidate/src/app/portal/dashboard/components/candidate-dashboard.tsx
  </files>
  <action>
    1. Create `use-upcoming-calls.ts` hook following the `use-candidate-dashboard-data.ts` pattern:
       - Use `useAuth()` + `createAuthenticatedClient` pattern from the candidate app
       - Fetch `GET /calls` with params: `{ status: 'scheduled', sort_by: 'scheduled_at', sort_order: 'asc', limit: 5 }`
       - Return `{ calls, loading, refresh }` — calls is an array of call objects with at minimum: `id`, `title`, `scheduled_at`, `participants`, `call_type`
       - Suppress `getToken` from useEffect deps (same pattern as other hooks, `// eslint-disable-line react-hooks/exhaustive-deps`)
       - Handle errors gracefully (log to console, return empty array)

    2. Create `upcoming-calls-widget.tsx` following the `match-preview-widget.tsx` pattern:
       - Props: `{ calls: CallListItem[], loading: boolean }`
       - Show a section header using the kicker pattern: `<h3 className="text-sm font-semibold uppercase tracking-[0.2em] text-base-content/40 mb-3">Upcoming Calls</h3>`
       - Loading state: skeleton with `loading loading-spinner`
       - Empty state: "No upcoming calls" message with `fa-phone-slash` icon
       - For each call, render a card with:
         - Call title (or "Call with [participant names]" if no title), using `text-base font-bold`
         - Scheduled date/time formatted with `toLocaleString()`, using `text-sm text-base-content/60`
         - Participant count or names, using `text-sm text-base-content/40`
         - "Join Call" link styled as `btn btn-primary btn-sm` linking to `/portal/calls/${call.id}/join`
       - Cards use `bg-base-100 border-2 border-base-200 p-5` (matching match-preview-widget pattern)
       - Cards have `border-l-4 border-primary` accent
       - Use Link from next/link for the join button

    3. Integrate into `candidate-dashboard.tsx`:
       - Import `useUpcomingCalls` hook and `UpcomingCallsWidget` component
       - Call `useUpcomingCalls()` at the top of the component alongside other data hooks
       - Add the widget as a new section between the Match Preview section and the Pipeline/Momentum section
       - Only render the section if `calls.length > 0 || callsLoading` (don't show empty section when no calls exist)
       - Follow the existing section pattern with `bg-base-200` or `bg-base-100` alternating background
  </action>
  <verify>
    Run `pnpm --filter @splits-network/candidate build` — should compile without errors.
    Verify the hook file exports `useUpcomingCalls`, the widget file exports a default component, and candidate-dashboard.tsx imports both.
  </verify>
  <done>
    Dashboard widget renders upcoming calls with title, time, participants, and Join Call button. Empty state handled gracefully. Widget integrated into candidate dashboard layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create join call page with token generation and redirect</name>
  <files>
    apps/candidate/src/hooks/use-call-token.ts
    apps/candidate/src/app/portal/calls/[id]/join/page.tsx
  </files>
  <action>
    1. Create `use-call-token.ts` hook (simplified version of portal's `use-create-call.ts`):
       - Only needs the `generateToken` function (candidates don't create calls)
       - Use `useAuth()` + `createAuthenticatedClient` pattern
       - `generateToken(callId: string)` calls `POST /calls/${callId}/token` and returns `{ access_token, livekit_token, call_id }`
       - Export the interface `CallTokenResult` with `access_token`, `livekit_token`, `call_id` fields
       - Return `{ generateToken, isLoading, error }`
       - Suppress `getToken` from deps (`// eslint-disable-line react-hooks/exhaustive-deps`)

    2. Create `apps/candidate/src/app/portal/calls/[id]/join/page.tsx` mirroring the portal's join page exactly:
       - `"use client"` directive at top
       - Use `useParams<{ id: string }>()` to get call ID
       - Use `useCallToken()` hook to get `generateToken`
       - In useEffect: call `generateToken(id)`, then redirect via `window.location.href = ${videoBaseUrl}/join/${result.access_token}`
       - `videoBaseUrl` = `process.env.NEXT_PUBLIC_VIDEO_APP_URL || "https://video.applicant.network"` — note the default is applicant.network (NOT splits.network like the portal)
       - Use `cancelled` flag pattern to prevent state updates after unmount (same as portal)
       - Error state: show error icon (`fa-circle-exclamation text-error`), error message, and "Back to Dashboard" link (not "Back to Call" since candidates don't have a calls list page)
       - Loading state: spinner with "Joining call..." text
       - Back link goes to `/portal/dashboard` (not `/portal/calls/${id}` since candidate app has no call detail page)
       - Suppress `generateToken` from useEffect deps (`// eslint-disable-line react-hooks/exhaustive-deps`)
  </action>
  <verify>
    Run `pnpm --filter @splits-network/candidate build` — should compile without errors.
    Verify the join page file exists at the correct App Router path.
    Grep for `NEXT_PUBLIC_VIDEO_APP_URL` in the join page to confirm it references video.applicant.network as default.
  </verify>
  <done>
    Join call page generates access token via POST /calls/{id}/token and redirects to video.applicant.network/join/{token}. Error state shows fallback UI with link back to dashboard. Default video URL is video.applicant.network (not splits.network).
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @splits-network/candidate build` passes
- Dashboard component imports and renders UpcomingCallsWidget
- Join page references `video.applicant.network` as default URL
- No hardcoded `splits.network` URLs in candidate app call files
</verification>

<success_criteria>
- Upcoming calls widget renders on candidate dashboard with call details and Join Call button
- Join call page generates token and redirects to video.applicant.network
- All files under 200 lines
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/52-candidate-call-experience/52-01-SUMMARY.md`
</output>
