---
phase: 52-candidate-call-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/candidate/src/app/portal/applications/components/shared/application-calls-section.tsx
  - apps/candidate/src/app/portal/applications/components/shared/application-detail.tsx
  - apps/candidate/src/lib/notifications.ts
autonomous: true

must_haves:
  truths:
    - "Application detail page shows entity-linked calls in a Calls section with call title, time, and status"
    - "Call notifications display phone icon instead of default bell icon"
  artifacts:
    - path: "apps/candidate/src/app/portal/applications/components/shared/application-calls-section.tsx"
      provides: "Read-only calls section component for application detail"
      exports: ["ApplicationCallsSection"]
    - path: "apps/candidate/src/lib/notifications.ts"
      provides: "Notification icon mapping including calls category"
      contains: "case 'calls'"
  key_links:
    - from: "apps/candidate/src/app/portal/applications/components/shared/application-calls-section.tsx"
      to: "/api/v2/calls"
      via: "useStandardList with entity_type=application filter"
      pattern: "entity_type.*application|useStandardList"
    - from: "apps/candidate/src/app/portal/applications/components/shared/application-detail.tsx"
      to: "apps/candidate/src/app/portal/applications/components/shared/application-calls-section.tsx"
      via: "import and render"
      pattern: "ApplicationCallsSection"
---

<objective>
Add a Calls section to the candidate application detail page showing entity-linked calls, and fix the notification icon for call notifications.

Purpose: Candidates need to see calls related to their applications (matching the portal's application call tab), and call notifications should show a recognizable phone icon.
Output: Application calls section component, updated application detail, notification icon fix.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/52-candidate-call-experience/52-RESEARCH.md

# Existing patterns to mirror
@apps/portal/src/app/portal/applications/components/shared/application-calls-tab.tsx
@apps/candidate/src/app/portal/applications/components/shared/application-detail.tsx
@apps/candidate/src/hooks/use-standard-list.ts
@apps/candidate/src/lib/notifications.ts
@apps/candidate/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create application calls section and integrate into detail page</name>
  <files>
    apps/candidate/src/app/portal/applications/components/shared/application-calls-section.tsx
    apps/candidate/src/app/portal/applications/components/shared/application-detail.tsx
  </files>
  <action>
    1. Create `application-calls-section.tsx` as a simplified, read-only version of the portal's `application-calls-tab.tsx`:
       - Props: `{ applicationId: string }`
       - Use `useAuth()` + `createAuthenticatedClient` to fetch calls
       - Fetch `GET /calls` with params: `{ entity_type: 'application', entity_id: applicationId, sort_by: 'scheduled_at', sort_order: 'desc', limit: 10 }`
       - Use the same `useAuth` + `createAuthenticatedClient` fetch pattern used elsewhere in the candidate app (check if `useStandardList` exists in candidate app — if yes, use it; if not, use a simple useEffect fetch pattern)
       - NO "New Call" button (candidates are read-only)
       - NO search/filter controls (keep it simple for secondary content)
       - Render a simple list of calls, each showing:
         - Call title (or fallback "Call" with participant names)
         - Date/time (`text-sm text-base-content/60`)
         - Status badge using DaisyUI `badge` component: `scheduled` = `badge-info`, `in_progress` = `badge-warning`, `completed` = `badge-success`, `cancelled` = `badge-error`
         - "Join" link for scheduled calls (Link to `/portal/calls/${call.id}/join`), only shown when status is `scheduled` and `scheduled_at` is in the future
       - Loading state: `loading loading-spinner loading-sm`
       - Empty state: "No calls yet" with `fa-phone-slash` icon, `text-sm text-base-content/40`
       - Section heading: `<h3 className="text-sm font-semibold uppercase tracking-[0.2em] text-base-content/40 mb-3">Calls</h3>`

    2. Integrate into `application-detail.tsx`:
       - Import `ApplicationCallsSection` from `./application-calls-section`
       - Add the component between the Notes section and the Timeline section
       - Pass `applicationId={application.id}`
       - Follow the existing section spacing pattern used by other sections in the file
       - Do NOT restructure the existing file — just add the import and render call at the appropriate location

    IMPORTANT: The `application-detail.tsx` is already ~700 lines. The calls section is extracted as a separate component file specifically to avoid adding bulk to this file. The integration should be just 2-3 lines added (import + render).
  </action>
  <verify>
    Run `pnpm --filter @splits-network/candidate build` — should compile without errors.
    Verify `application-calls-section.tsx` exists and is under 200 lines.
    Verify `application-detail.tsx` imports and renders `ApplicationCallsSection`.
  </verify>
  <done>
    Application detail page shows a Calls section with entity-linked calls. Each call shows title, date, status badge, and Join link for scheduled calls. Component is read-only (no call creation). Section appears between Notes and Timeline.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add calls category to notification icon mapping</name>
  <files>
    apps/candidate/src/lib/notifications.ts
  </files>
  <action>
    Add `case 'calls': return 'fa-phone';` to the `getNotificationIcon()` switch statement in `apps/candidate/src/lib/notifications.ts`, before the `default` case.

    This ensures call-related in-app notifications (created by the notification-service with `category: 'calls'`) display a phone icon instead of the fallback bell icon in the candidate notification page.
  </action>
  <verify>
    Grep `notifications.ts` for `case 'calls'` to confirm the case was added.
    Run `pnpm --filter @splits-network/candidate build` — should compile without errors.
  </verify>
  <done>
    Call notifications in the candidate app's notification page display a phone icon (`fa-phone`) instead of the default bell icon.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @splits-network/candidate build` passes
- Application detail imports and renders ApplicationCallsSection
- application-calls-section.tsx is under 200 lines
- notifications.ts has `case 'calls'` returning `fa-phone`
</verification>

<success_criteria>
- Application detail page shows calls section with entity-linked calls
- Calls section is read-only (no creation button)
- Call notifications show phone icon
- All new files under 200 lines
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/52-candidate-call-experience/52-02-SUMMARY.md`
</output>
