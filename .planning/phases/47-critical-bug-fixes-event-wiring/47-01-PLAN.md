---
phase: 47-critical-bug-fixes-event-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/video/src/components/join-flow.tsx
  - services/notification-service/src/domain-consumer.ts
  - services/notification-service/src/consumers/calls/consumer.ts
autonomous: true

must_haves:
  truths:
    - "After token exchange, session data is stored and read with the same key — users can join video calls"
    - "Recording-ready events from video-service are received by notification-service and trigger email/in-app notifications"
    - "24h and 1h reminder emails are sent for scheduled calls (not just the 5-min reminder)"
  artifacts:
    - path: "apps/video/src/components/join-flow.tsx"
      provides: "SessionStorage write with correct key format"
      contains: "call-${result.call.id}"
    - path: "services/notification-service/src/domain-consumer.ts"
      provides: "Correct event bindings for call.recording_ready and call.reminder"
      contains: "call.recording_ready"
    - path: "services/notification-service/src/consumers/calls/consumer.ts"
      provides: "Reminder event handler"
      contains: "handleReminder"
  key_links:
    - from: "apps/video/src/components/join-flow.tsx"
      to: "apps/video/src/app/call/[callId]/page.tsx"
      via: "sessionStorage key format"
      pattern: "call-\\$\\{.*\\}"
    - from: "services/video-service/src/v2/calls/call-recording-webhook.ts"
      to: "services/notification-service/src/domain-consumer.ts"
      via: "RabbitMQ event name"
      pattern: "call\\.recording_ready"
    - from: "services/call-service/src/v2/scheduler.ts"
      to: "services/notification-service/src/domain-consumer.ts"
      via: "RabbitMQ event name"
      pattern: "call\\.reminder"
---

<objective>
Fix three critical bugs blocking end-to-end video call flows: a sessionStorage key mismatch that prevents joining calls, an event name mismatch that silences recording notifications, and a missing event binding that drops 24h/1h reminders.

Purpose: These are P0/P1 bugs identified in the v10.0 milestone audit. Without these fixes, users cannot join video calls, never receive recording-ready notifications, and miss 24h/1h reminder emails.

Output: Three targeted fixes across video app and notification-service that restore full call flow functionality.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/video/src/components/join-flow.tsx
@apps/video/src/app/call/[callId]/page.tsx
@services/notification-service/src/domain-consumer.ts
@services/notification-service/src/consumers/calls/consumer.ts
@services/video-service/src/v2/calls/call-recording-webhook.ts
@services/call-service/src/v2/scheduler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sessionStorage key mismatch in video app join flow</name>
  <files>apps/video/src/components/join-flow.tsx</files>
  <action>
    In `apps/video/src/components/join-flow.tsx`, line 23, change the sessionStorage key from colon separator to hyphen separator:

    Change: `call:${result.call.id}`
    To: `call-${result.call.id}`

    This matches the reader in `apps/video/src/app/call/[callId]/page.tsx` line 24 which reads `call-${callId}`.

    Only this one character change is needed. Do NOT modify the call page reader.
  </action>
  <verify>
    Run `grep -n "call-\${" apps/video/src/components/join-flow.tsx` — should show the corrected key on line 23.
    Run `grep -n "call:" apps/video/src/components/join-flow.tsx` — should return NO matches (the colon key is gone).
    Run `pnpm --filter @splits-network/video build` — build succeeds.
  </verify>
  <done>join-flow.tsx writes sessionStorage key `call-${id}` matching the `call-${callId}` reader in the call page — users can join video calls after token exchange.</done>
</task>

<task type="auto">
  <name>Task 2: Fix recording event name mismatch and add reminder binding in notification-service</name>
  <files>services/notification-service/src/domain-consumer.ts, services/notification-service/src/consumers/calls/consumer.ts</files>
  <action>
    **Fix 1 — Recording event name (domain-consumer.ts):**

    In `services/notification-service/src/domain-consumer.ts`, find the binding on line ~422:
    ```
    await this.channel.bindQueue(this.queue, this.exchange, 'call.recording.ready');
    ```
    Change to:
    ```
    await this.channel.bindQueue(this.queue, this.exchange, 'call.recording_ready');
    ```
    This matches the publisher in video-service `call-recording-webhook.ts` line 51 which publishes `call.recording_ready` (underscore, not dot).

    Also update the switch case in `handleEvent()` on line ~817:
    ```
    case 'call.recording.ready':
    ```
    Change to:
    ```
    case 'call.recording_ready':
    ```

    **Fix 2 — Add call.reminder binding (domain-consumer.ts):**

    Add a new binding after the `call.starting_soon` binding (line ~423):
    ```
    await this.channel.bindQueue(this.queue, this.exchange, 'call.reminder');
    ```

    Add a new case in `handleEvent()` after the `call.starting_soon` case (line ~820):
    ```
    case 'call.reminder':
        await this.callsConsumer.handleReminder(event);
        break;
    ```

    **Fix 3 — Add handleReminder method (calls/consumer.ts):**

    Add a `handleReminder` method to `CallsEventConsumer` in `services/notification-service/src/consumers/calls/consumer.ts`. Place it after the `handleStartingSoon` method. The reminder payload from call-service scheduler includes: `call_id`, `reminder_type` ('24h' | '1h'), `call_type`, `title`, `scheduled_at`, `participants`.

    The handler should:
    1. Normalize the payload using `this.normalizePayload(event.payload)` (add `reminderType` to the CallEventPayload interface: `reminderType?: string;` and normalize it from `raw.reminderType || raw.reminder_type`)
    2. Resolve participant contacts
    3. Determine the `timeUntil` string: if `reminderType` is '24h' use '24 hours', if '1h' use '1 hour'
    4. Resolve entity context and brand
    5. Call `this.emailService.sendReminder()` for each contact (same method used by handleStartingSoon), passing:
       - `title`, `participantNames`, `dateTime` (formatted scheduledAt), `timeUntil`, `joinUrl` (portal URL), `entityContext`, `source`, `userId`
    6. Do NOT send in-app toast for reminders (those are only for the 5-min starting_soon event)
    7. Log success with callId, recipientCount, and reminderType
  </action>
  <verify>
    Run `grep -n "call.recording_ready" services/notification-service/src/domain-consumer.ts` — should show the corrected binding AND switch case (2 matches).
    Run `grep -n "call.recording.ready" services/notification-service/src/domain-consumer.ts` — should return NO matches.
    Run `grep -n "call.reminder" services/notification-service/src/domain-consumer.ts` — should show 1 binding + 1 switch case (2 matches).
    Run `grep -n "handleReminder" services/notification-service/src/consumers/calls/consumer.ts` — should show the new method.
    Run `pnpm --filter @splits-network/notification-service build` — build succeeds.
  </verify>
  <done>
    notification-service binds `call.recording_ready` (matching video-service publisher) so recording notifications fire.
    notification-service binds `call.reminder` (matching call-service scheduler) and routes to handleReminder which sends 24h/1h reminder emails via the existing sendReminder email template.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "call:" apps/video/src/components/join-flow.tsx` returns no matches (colon key eliminated)
2. `grep -rn "call-\\\${" apps/video/src/components/join-flow.tsx` returns the corrected hyphen key
3. `grep -rn "call.recording.ready" services/notification-service/` returns no matches (dot format eliminated)
4. `grep -rn "call.recording_ready" services/notification-service/` returns matches in domain-consumer.ts
5. `grep -rn "call.reminder" services/notification-service/` returns binding + switch case in domain-consumer.ts
6. `pnpm --filter @splits-network/video build` succeeds
7. `pnpm --filter @splits-network/notification-service build` succeeds
</verification>

<success_criteria>
- SessionStorage key uses hyphen separator in both writer (join-flow.tsx) and reader (call page) — video calls joinable
- Recording event binding uses underscore format `call.recording_ready` matching video-service publisher — recording notifications fire
- Reminder event `call.reminder` is bound and handled, sending 24h/1h emails via existing sendReminder template — reminder emails delivered
- Both apps/services build successfully with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/47-critical-bug-fixes-event-wiring/47-01-SUMMARY.md`
</output>
