---
phase: 44-recruiter-company-calls-portal-integration
plan: 09
type: execute
wave: 4
depends_on: ["44-06", "44-07"]
files_modified:
  - apps/portal/src/app/portal/companies/components/shared/company-calls-tab.tsx
  - apps/portal/src/app/portal/roles/components/shared/job-calls-tab.tsx
  - apps/portal/src/app/portal/companies/components/split/company-detail.tsx
  - apps/portal/src/app/portal/roles/components/shared/job-detail.tsx
autonomous: true

must_haves:
  truths:
    - "Company detail page has a Calls tab showing calls linked to that company"
    - "Job detail page has a Calls tab showing calls linked to that job"
    - "Entity-scoped call tabs have full filtering (not simplified)"
    - "Call initiation button on entity pages opens creation modal with entity pre-filled"
  artifacts:
    - path: "apps/portal/src/app/portal/companies/components/shared/company-calls-tab.tsx"
      provides: "Calls tab for company detail pages"
    - path: "apps/portal/src/app/portal/roles/components/shared/job-calls-tab.tsx"
      provides: "Calls tab for job detail pages"
  key_links:
    - from: "company-calls-tab.tsx"
      to: "/api/v2/calls"
      via: "Fetch with entity_type=company&entity_id={companyId}"
      pattern: "entity_type=company"
    - from: "job-calls-tab.tsx"
      to: "/api/v2/calls"
      via: "Fetch with entity_type=job&entity_id={jobId}"
      pattern: "entity_type=job"
---

<objective>
Add Calls tabs to company and job detail pages with full filtering, plus "New Call" buttons that open the creation modal pre-filled with the entity context.

Purpose: Entity-scoped call visibility so users see relevant calls in context (HIST-01 entity tabs).
Output: Two new tab components integrated into existing detail views.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-recruiter-company-calls-portal-integration/44-CONTEXT.md
@apps/portal/src/app/portal/companies/components/split/company-detail.tsx
@apps/portal/src/app/portal/roles/components/shared/job-detail.tsx
@apps/portal/src/app/portal/calls/components/
@apps/portal/src/components/calls/call-creation-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create entity-scoped call tabs for company and job detail pages</name>
  <files>
    apps/portal/src/app/portal/companies/components/shared/company-calls-tab.tsx
    apps/portal/src/app/portal/roles/components/shared/job-calls-tab.tsx
  </files>
  <action>
Both tab components follow the same pattern — parameterized by entity type and ID.

**company-calls-tab.tsx:**
- Props: `companyId: string`, `companyName: string`
- Fetch calls from `/api/v2/calls?entity_type=company&entity_id={companyId}` with pagination
- Full filter bar (reuse `CallFilters` component from 44-06, but with entity pre-locked)
- Table view by default (compact, fits in a tab panel)
- "New Call" button at top-right that opens CallCreationModal with `defaultEntityType='company'` and `defaultEntityId={companyId}`
- Each row links to call detail page (`/portal/calls/{id}`)
- Shows stats summary: total calls, last call date

**job-calls-tab.tsx:**
- Props: `jobId: string`, `jobTitle: string`
- Same pattern as company but with `entity_type=job&entity_id={jobId}`
- "New Call" button pre-fills with job entity
- Table view with filters
- Stats: total calls for this job, last call date
  </action>
  <verify>Run `pnpm --filter @splits-network/portal build` to verify TypeScript compiles.</verify>
  <done>Both entity-scoped call tabs fetch and display entity-filtered calls with full filtering and "New Call" button.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate call tabs into company and job detail views</name>
  <files>
    apps/portal/src/app/portal/companies/components/split/company-detail.tsx
    apps/portal/src/app/portal/roles/components/shared/job-detail.tsx
  </files>
  <action>
**In company-detail.tsx:**
- Read the existing file first to understand the current tab structure
- Add a "Calls" tab to the existing DaisyUI tabs alongside other tabs (Overview, Jobs, Contacts, etc.)
- Tab content renders `CompanyCallsTab` with the company ID and name props
- Position the Calls tab logically (after main info tabs, before settings if applicable)

**In job-detail.tsx:**
- Read the existing file first to understand the current tab structure (likely has tabs like Brief, Candidates, Company, Financials based on the component names we saw)
- Add a "Calls" tab to the existing DaisyUI tabs
- Tab content renders `JobCallsTab` with the job ID and title props
- Position after existing content tabs

Both modifications should be minimal — just adding the tab trigger and tab content panel. Import the new tab components.

Also add "Call" action button to the entity detail headers:
- Company detail header: add a phone/video icon button next to existing actions that opens CallCreationModal with company pre-filled
- Job detail header: same pattern with job pre-filled
- Use `fa-duotone fa-regular fa-video` icon, DaisyUI `btn btn-sm` styling
  </action>
  <verify>Run `pnpm --filter @splits-network/portal build` to verify TypeScript compiles.</verify>
  <done>Company and job detail pages have Calls tabs and call initiation buttons. Tabs render entity-scoped call lists. Buttons open creation modal with entity context pre-filled.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @splits-network/portal build` passes
- Company detail has Calls tab rendering CompanyCallsTab
- Job detail has Calls tab rendering JobCallsTab
- Entity filters are pre-locked in scoped tabs
- New Call buttons open modal with correct entity defaults
</verification>

<success_criteria>
Entity-scoped call tabs are visible on company and job detail pages with full filtering. Call initiation is available from entity pages with smart defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/44-recruiter-company-calls-portal-integration/44-09-SUMMARY.md`
</output>
