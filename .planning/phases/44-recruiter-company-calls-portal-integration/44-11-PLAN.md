---
phase: 44-recruiter-company-calls-portal-integration
plan: 11
type: execute
wave: 5
depends_on: ["44-08", "44-10"]
files_modified:
  - apps/video/src/components/call-ended.tsx
  - apps/video/src/components/post-call-summary.tsx
  - apps/portal/src/components/chat/chat-widget-header.tsx
  - apps/portal/src/components/calls/call-creation-modal.tsx
autonomous: true

must_haves:
  truths:
    - "Post-call summary screen shows duration, participants, and link to recording"
    - "Schedule follow-up action pre-fills creation modal with same participants and entity"
    - "View in Portal link navigates to call detail page"
    - "Chat widget header has call icon for global call initiation"
    - "Active chat header call icon pre-fills with chat recipient"
    - "Chat list header call icon opens blank creation form"
  artifacts:
    - path: "apps/video/src/components/post-call-summary.tsx"
      provides: "Post-call summary screen in video app"
    - path: "apps/portal/src/components/chat/chat-widget-header.tsx"
      provides: "Chat widget with call action button"
  key_links:
    - from: "post-call-summary.tsx"
      to: "portal calls detail"
      via: "View in Portal link"
      pattern: "portal/calls/{callId}"
    - from: "chat-widget-header.tsx"
      to: "call-creation-modal.tsx"
      via: "Opens modal on call icon click"
      pattern: "CallCreationModal|setShowCallModal"
---

<objective>
Build the post-call summary screen in the video app and integrate call initiation into the portal's chat widget header for global call access.

Purpose: Smooth post-call experience with follow-up actions (EXP-01), and global call initiation from chat context (CALL-01).
Output: Post-call screen + chat widget call integration.
</objective>

<execution_context>
@./.claude/basel-gsd/workflows/execute-plan.md
@./.claude/basel-gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-recruiter-company-calls-portal-integration/44-CONTEXT.md
@apps/video/src/components/call-ended.tsx
@apps/portal/src/components/chat/
@apps/portal/src/components/calls/call-creation-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create post-call summary screen in video app</name>
  <files>
    apps/video/src/components/call-ended.tsx
    apps/video/src/components/post-call-summary.tsx
  </files>
  <action>
**Read existing `call-ended.tsx` first** to understand the current end-of-call screen. Enhance it or replace with post-call summary.

**post-call-summary.tsx:**
- Shown after a call ends (replaces or enhances the current call-ended screen)
- Content:
  - **Call duration**: formatted as "Xh Ym" or "Ym Zs"
  - **Participant list**: avatars + names of all participants
  - **Recording status**: "Recording processing..." if pending, or "Recording available" with link if ready
  - **Entity context**: company/job name if linked
  - **Tags**: displayed as badges if any were set
- Actions:
  1. **"Schedule Follow-up"** button — opens a simplified scheduling form OR redirects to portal with call creation modal pre-filled (same participants, same entity). Since we're in the video app (no portal modal available), redirect to portal URL: `${portalUrl}/portal/calls?action=new&participants={ids}&entity_type={type}&entity_id={id}`
  2. **"View in Portal"** link — opens `${portalUrl}/portal/calls/{callId}` in new tab
  3. **"New Call"** button — start another call (redirect to join page or back to portal)
  4. **"Close"** button — closes the video app tab/window
- Brand-aware: uses the same brand detection from the video app

**Update call-ended.tsx:**
- If the component currently shows a basic "call ended" message, replace it with `PostCallSummary`
- Pass call data (callId, duration, participants, entity links) as props
- Fetch call detail if not already available in state
  </action>
  <verify>Run `pnpm --filter @splits-network/video build` to verify TypeScript compiles.</verify>
  <done>Post-call summary screen shows call details with recording status, schedule follow-up action, and view in portal link.</done>
</task>

<task type="auto">
  <name>Task 2: Add call initiation to chat widget header</name>
  <files>
    apps/portal/src/components/chat/chat-widget-header.tsx
  </files>
  <action>
**Read the existing chat widget header component first** to understand its structure. The file might be named differently — check the chat components directory.

**Look for:**
- The chat widget/drawer component
- The header of the chat list (shown when no active chat)
- The header of an active chat conversation

**Add call icon buttons:**

1. **Chat list header** (when viewing chat list, no active conversation):
   - Add a video/phone icon button: `<i className="fa-duotone fa-regular fa-video" />`
   - DaisyUI `btn btn-ghost btn-sm` styling
   - On click: open CallCreationModal with NO defaults (blank form — user picks participant and entity)
   - Position: in the header actions area alongside other action buttons

2. **Active chat header** (when in a conversation with someone):
   - Add same video icon button
   - On click: open CallCreationModal with:
     - `defaultParticipants` pre-filled with the chat recipient(s)
     - If the chat has entity context (linked candidate, job, application): pre-fill entity
   - This creates a seamless chat-to-call flow

**Integration:**
- Import CallCreationModal component
- Manage modal open state with `useState`
- Pass default props based on current chat context
- The chat system's existing context (linked candidates, recruiters, applications) flows into call context automatically when call initiated from chat — no re-linking needed (per CONTEXT.md)

If the chat components use a different architecture (e.g., the widget is a portal-level component, not in `src/components/chat/`), follow wherever the chat header actually lives. Search for the chat widget in the portal app.
  </action>
  <verify>Run `pnpm --filter @splits-network/portal build` to verify TypeScript compiles.</verify>
  <done>Chat widget has call icon in both list and active chat headers. Clicking opens call creation modal with appropriate defaults based on chat context.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @splits-network/video build` passes
- `pnpm --filter @splits-network/portal build` passes
- Post-call screen shows after call ends
- Schedule Follow-up redirects to portal with pre-filled params
- View in Portal opens correct detail page
- Chat widget call icons visible in both header states
</verification>

<success_criteria>
Post-call experience provides follow-up actions. Chat widget enables call initiation from any chat context with smart defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/44-recruiter-company-calls-portal-integration/44-11-SUMMARY.md`
</output>
