---
phase: 02-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/portal/src/app/public/pricing/pricing-content.tsx
autonomous: true

must_haves:
  truths:
    - "Hero section has gradient background matching landing page style"
    - "Headline text animates with staggered reveal on scroll"
    - "Hero respects prefers-reduced-motion preference"
  artifacts:
    - path: "apps/portal/src/app/public/pricing/pricing-content.tsx"
      provides: "Hero section with gradient background and animated headline"
      contains: "bg-gradient"
  key_links:
    - from: "pricing-content.tsx"
      to: "animation-utils.ts"
      via: "duration, easing, stagger imports"
      pattern: "import.*duration.*easing.*stagger.*animation-utils"
---

<objective>
Redesign the pricing page hero section to match the landing page visual style with gradient background and animated headline reveal.

Purpose: The hero is the first thing users see - matching the landing page establishes visual continuity and sets the premium tone.
Output: Hero section with gradient background and staggered headline animation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-animation-infrastructure/01-01-SUMMARY.md
@apps/portal/src/app/public/pricing/pricing-content.tsx
@apps/portal/src/components/landing/sections/hero-section.tsx
@apps/portal/src/components/landing/shared/animation-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign hero section with gradient background</name>
  <files>apps/portal/src/app/public/pricing/pricing-content.tsx</files>
  <action>
Replace the current hero section styling with a gradient background that matches the landing page aesthetic.

Current: `bg-secondary text-secondary-content`
Change to: Gradient background using Tailwind classes like `bg-gradient-to-br from-primary via-primary/90 to-secondary` with `text-primary-content`.

Key changes:
1. Update the section className to use gradient: `bg-gradient-to-br from-primary via-primary/90 to-secondary`
2. Add relative positioning for potential future overlay effects
3. Increase hero min-height to `min-h-[60vh]` for more visual impact
4. Add subtle overlay div with `absolute inset-0 bg-black/5` for depth
5. Ensure content has `relative z-10` to sit above overlay
6. Update text colors to `text-primary-content` for contrast

Reference the landing page hero-section.tsx for the visual style but adapt for pricing context (no video, gradient instead).
  </action>
  <verify>
Visual inspection: Run `pnpm --filter @splits-network/portal dev` and visit http://localhost:3100/public/pricing. Hero should have gradient background from primary to secondary colors.
  </verify>
  <done>
Hero section displays gradient background matching landing page color scheme, with proper text contrast.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add staggered headline animation</name>
  <files>apps/portal/src/app/public/pricing/pricing-content.tsx</files>
  <action>
Enhance the hero animation to include staggered reveal of headline elements, similar to landing page hero.

Current state: Simple fadeUp on heroContentRef
Target: Separate animations for headline, subtitle, with stagger timing

Implementation:
1. Import `stagger` from animation-utils (add to existing import)
2. Create separate refs for each element:
   - `headlineRef` for the h1
   - `subtitleRef` for the description paragraph
3. Update useGSAP to animate elements in sequence using a timeline:
   ```typescript
   const tl = gsap.timeline({
     scrollTrigger: {
       trigger: heroRef.current,
       start: 'top 80%',
     },
   });

   // Headline - fade up with slight scale
   tl.fromTo(
     headlineRef.current,
     { opacity: 0, y: 40, scale: 0.98 },
     { opacity: 1, y: 0, scale: 1, duration: duration.hero, ease: easing.smooth }
   );

   // Subtitle - fade up, overlapping with headline
   tl.fromTo(
     subtitleRef.current,
     { opacity: 0, y: 30 },
     { opacity: 1, y: 0, duration: duration.normal, ease: easing.smooth },
     '-=0.5' // Start before headline finishes
   );
   ```
4. Add `opacity-0` class to headlineRef and subtitleRef elements (initial hidden state)
5. Remove the heroContentRef wrapper animation (replaced by individual animations)

Pattern follows landing page hero-section.tsx entranceTl pattern but simplified for scroll-triggered context.
  </action>
  <verify>
1. Build check: `pnpm --filter @splits-network/portal build` passes without errors
2. Visual test: Scroll to hero section - headline should animate first (larger, with scale), then subtitle overlaps
3. Accessibility: With `prefers-reduced-motion` enabled in OS, no animations should play
  </verify>
  <done>
Hero headline and subtitle animate in staggered sequence on scroll, with headline having subtle scale effect. Animation respects reduced motion preference.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @splits-network/portal build` completes without errors
2. Dev server shows gradient hero with animated headline on scroll
3. Animation timing feels cohesive with landing page (same easing, similar durations)
4. Reduced motion check prevents all animations
</verification>

<success_criteria>
- Hero section has gradient background (from-primary to-secondary)
- Headline animates with fadeUp + slight scale on scroll trigger
- Subtitle animates after headline with overlap
- All animations use animation-utils presets (duration.hero, easing.smooth, etc.)
- prefers-reduced-motion disables all animations
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-redesign/02-01-SUMMARY.md`
</output>
