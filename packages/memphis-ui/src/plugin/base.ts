/** CSS Custom Properties — single source of truth for border tiers */
export const baseStyles = {
    ':root': {
        '--border-container': '4px',
        '--border-interactive': '3px',
        '--border-detail': '2px',
    },
};
