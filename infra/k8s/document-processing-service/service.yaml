apiVersion: v1
kind: Service
metadata:
  name: document-processing-service
  namespace: splits-network
  labels:
    app: document-processing-service
spec:
  selector:
    app: document-processing-service
  ports:
  - name: http
    port: 3008
    targetPort: 3008
    protocol: TCP
  type: ClusterIP