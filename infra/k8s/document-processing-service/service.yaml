apiVersion: v1
kind: Service
metadata:
  name: document-processing-service
  namespace: splits-network
  labels:
    app: document-processing-service
spec:
  selector:
    app: document-processing-service
  ports:
  - name: http
    port: 80
    targetPort: 3006
    protocol: TCP
  type: ClusterIP