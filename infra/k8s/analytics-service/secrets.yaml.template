# This is a template for secrets. Copy to secrets.yaml and fill in actual values.
# DO NOT commit secrets.yaml to git!

apiVersion: v1
kind: Secret
metadata:
  name: analytics-service-secrets
  namespace: splits-network
type: Opaque
stringData:
  # Add any analytics-service specific secrets here
  # For now, all secrets come from shared secrets (supabase-secrets, internal-service-secrets)
