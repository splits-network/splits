apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-service
  namespace: splits-network
spec:
  replicas: 2
  selector:
    matchLabels:
      app: content-service
  template:
    metadata:
      labels:
        app: content-service
    spec:
      containers:
        - name: content-service
          image: ${ACR_SERVER}/content-service:${IMAGE_TAG}
          ports:
            - containerPort: 3015
              name: http
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3015"
            - name: SUPABASE_URL
              valueFrom:
                secretKeyRef:
                  name: supabase-secrets
                  key: supabase-url
            - name: SUPABASE_ANON_KEY
              valueFrom:
                secretKeyRef:
                  name: supabase-secrets
                  key: supabase-anon-key
            - name: SUPABASE_SERVICE_ROLE_KEY
              valueFrom:
                secretKeyRef:
                  name: supabase-secrets
                  key: supabase-service-role-key
            - name: RABBITMQ_URL
              value: "amqp://splits:splits_production@rabbitmq:5672"
            - name: INTERNAL_SERVICE_KEY
              valueFrom:
                secretKeyRef:
                  name: internal-service-secrets
                  key: internal-service-key
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3015
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3015
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: content-service
  namespace: splits-network
spec:
  type: ClusterIP
  ports:
    - port: 3015
      targetPort: 3015
      name: http
  selector:
    app: content-service
