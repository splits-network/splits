apiVersion: v1
kind: Service
metadata:
    name: clamav
    namespace: splits-network
    labels:
        app: clamav
spec:
    ports:
        - port: 3310
          targetPort: 3310
          name: clamd
    selector:
        app: clamav
